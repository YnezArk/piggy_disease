# 诊疗决策板块 — 整体运行流程说明

> 面向：全团队（含前端、整合板块成员）
> 目标：讲清"系统从输入到输出的完整运行流程"，以及每个环节对应哪个文件、读写哪些数据
> 配套：技术路线报告（架构设计）、实操手册（怎么实现）、测试手册（怎么验证）

---

## 一、系统全景：诊疗决策在整体系统中的位置

```
┌─────────────────────────────────────────────────────────────────────┐
│                    音甘猪咳嗽智能诊疗系统（整体）                       │
│                                                                     │
│  ┌──────────┐    ┌─────────────────────────────┐    ┌──────────┐   │
│  │ 监测板块   │    │    诊疗决策板块（本板块）        │    │ 整合板块   │   │
│  │ 杨宇航/郭悦 │──▶│  辨病 → 辨证 → 组方 → 校验     │──▶│ 周海斌     │──▶ 前端小程序
│  │ 咳嗽识别    │    │                             │    │ 前后端对接  │   │
│  └──────────┘    │  认知层·辨病  │ 认知层·论治     │    └──────────┘   │
│                  │  (分类模型)    │  (LLM+RAG)     │                   │
│                  └───────────────┴───────────────┘                   │
│                                                                     │
│  数据底座：MySQL pig_diag（知识库+业务记录） + rag_retrieve 内存检索   │
└─────────────────────────────────────────────────────────────────────┘
```

**一句话**：监测板块告诉我们"猪在咳嗽"，诊疗板块告诉养殖户"咳的什么病、怎么治"。

---

## 二、整体数据流（一图看懂）

```
                    ┌───────────── 输入层 ─────────────┐
                    │  ① 监测模块：咳嗽特征+体温+精神状态  │
                    │  ② 人工输入：症状描述/体重/严重度    │
                    └──────────────┬──────────────────┘
                                   ▼
                    ┌───────────── 辨病层（认知层·辨病）──────────────┐
                    │  辨病模型（SVM/CNN-BiLSTM 多分类）               │
                    │  → 输出：疾病诊断 + 置信度（如：支原体肺炎 0.91）  │
                    │  ※ 音频数据就绪前，用人工指定疾病名代替            │
                    └──────────────┬──────────────────┘
                                   ▼
                    ┌───────────── 论治层（认知层·论治）──────────────┐
                    │                                               │
                    │  ┌─ RAG 三库检索（rag_retrieve.py，内存余弦）──┐ │
                    │  │  ① 辨证库(symptom_disease)  → 体征→证候佐证  │ │
                    │  │  ② 论治库(disease_formula)  → 证候→基础方   │ │
                    │  │  ③ 典籍库(tcm_classics)     → 治则/用药依据  │ │
                    │  └──────────────┬────────────────┘             │
                    │                 ▼                              │
                    │  ┌─ 辨证Agent（LLM：qwen3.7-plus）───────────┐ │
                    │  │  输入：辨病结果+症状+检索片段               │ │
                    │  │  输出：分期/证型/治则/依据/引用(JSON)       │ │
                    │  └──────────────┬────────────────┘             │
                    │                 ▼                              │
                    │  ┌─ 组方Agent（LLM）─────────────────────────┐ │
                    │  │  输入：辨证结果+检索到的基础方剂            │ │
                    │  │  输出：方剂/药材剂量/用法疗程/禁忌/引用(JSON)│ │
                    │  └──────────────┬────────────────┘             │
                    │                 ▼                              │
                    │  ┌─ 校验Agent（safety.py 规则引擎，非LLM）────┐ │
                    │  │  ① 十八反/十九畏查表    → 硬拦截           │ │
                    │  │  ② 剂量超8倍上限(幻觉)  → 硬拦截           │ │
                    │  │  ③ 超人用参考区间/孕猪  → 软提示           │ │
                    │  └──────────────────────────────────────────┘ │
                    └──────────────┬──────────────────┘
                                   ▼
                    ┌───────────── 输出层 ─────────────┐
                    │  诊疗报告：证型/治则/组方/用法/禁忌/引用 │
                    │  → 前端展示（小程序/网页）            │
                    │  → 落库（diagnosis_record +         │
                    │     prescription_record）           │
                    └──────────────┬──────────────────┘
                                   ▼
                    ┌───────────── 反馈闭环 ─────────────┐
                    │  疗效追踪（treatment_feedback）      │
                    │  → 有效方剂反哺论治库（更新权重/规则） │
                    └──────────────────────────────────┘
```

---

## 三、详细流程分解

### 3.1 输入层

| 来源 | 内容 | 说明 |
|------|------|------|
| 监测模块 | 咳嗽频率/强度 + 体温 + 精神状态 | 整合板块对接后传入；当前用人工输入模拟 |
| 人工输入 | 症状描述、体重、严重度、猪舍 | 交互终端/API 均可 |

### 3.2 辨病层（认知层·辨病）

- **模型**：SVM/CNN-BiLSTM 多分类（数据量要求低）
- **输出**：`疾病名 + 置信度`，如 `猪支原体肺炎 0.91`
- **现状**：音频数据在监测板块，模型待其提供样本后训练；**当前用人工指定疾病名走通论治链路**
- 辨病结果进入论治层时附带：疾病名、症状、体温、体重、严重度

### 3.3 论治层（认知层·论治）—— 核心五步

**Step 0 查询改写**（LLM Query Rewriter，temperature=0.0）
```
LLM 根据输入特征生成检索指令（对齐《探讨预期》"第一步：大模型根据输入特征生成查询指令"）：
输入：口语化描述 "猪老咳个不停，没精神，吃不下饭，便便有点稀"
输出：{"syndrome_query":"连咳 精神差 食欲下降 稀溏",   ← 规范化检索词
      "classic_query":"猪支原体肺炎 连咳 辨证 治则",
      "temp_range":"38.5-40"}                            ← 体温区间（供SQL精确检索）
改写失败时自动回退用原始输入（兜底不崩）
```

**Step 1 知识注入（分层策略，`FULL_KB=True` 默认开启）**
```
小库全量注入（语义匹配交给 LLM，比弱向量检索更准）：
  辨证库 9 条规则 全量 → LLM 逐一匹配，全局权衡（含主症优先原则）
  论治库 14 首方剂 全量 → LLM 自选最匹配方剂
大库保持检索（数据大，全量约15k+ token 不划算）：
  典籍库 230 段 → Top5 检索（查询词来自 Step0 改写）
Text-to-SQL 精确检索（MySQL 辨证库）→ 体温区间+关键词精确命中，证据增强
命中片段带【来源】标签 = "开卷考试资料"
注：FULL_KB=False 可回退旧检索模式（数据量变大时启用）
```

**Step 2 辨证Agent**（LLM，temperature=0.0 求确定性）+ **辨病纠错（双重交叉验证）**
```
输入：辨病结果（标注"参考，可能不准确"）+ 症状 + 辨证库全量规则 + 典籍检索片段 + SQL精确命中
⚠️ 提示词硬约束：
  1. 辨证以症状体征和检索证据为准，辨病结果仅作参考（同病异证，证候由体征决定）
  2. 症状与辨病矛盾时必须按症状判定，并在 disease_conflict 中明确指出
  3. 主症优先：咳嗽/体温/呼吸定证，精神/食欲/排泄物佐证
  4. 特异体征优先：犬坐/张口呼吸/血性泡沫 = 肺气壅闭重症标志（疫热壅肺），
     权重高于"突发高热"等泛化体征——同时出现时按重症特异体征定证
输出：{"stage":"中期","syndrome":"疫热壅肺","principle":"清热宣肺化痰平喘",
      "evidence":[...],"references":[...],
      "disease_conflict":{"upstream_disease":"上游辨病","evidence_disease":"证据指向疾病",
                          "conflict":true/false,"reason":"矛盾说明"}}

双重纠错机制（上游辨病出错时）：
  ① 确定性规则验证（rule_cross_check，非LLM）：
     仅凭体征查辨证库规则（多关键词OR+evidence字段）→ 全部命中规则映射疾病
     vs 上游疾病名 → 任一不一致即 conflict=true（列出全部矛盾规则）
  ② LLM 独立判断（disease_conflict）：
     辨证Agent 按症状独立判定证据指向的疾病，与上游对比，矛盾时给出修正建议
  实测：上游错判"支原体肺炎"，症状为"突发高热40.5℃、全群爆发"
        → 规则验证+LLM双重指出矛盾，辨证按症状定为邪袭肺卫，建议 RT-PCR 确诊，组方银翘散 ✅
  实测：上游"猪流感"，症状"42℃+犬坐" → 辨证疫热壅肺（不被锚定），
        证据指向胸膜肺炎，双重警告与上游矛盾 ✅
```

**Step 3 组方Agent**（LLM，temperature=0.5 允许合理变化）
```
输入：辨证结果 + 检索到的基础方剂（清肺散：板蓝根30/浙贝母20/葶苈子25/桔梗20/甘草15）
推理：证型匹配 → 采用清肺散，按病情/体重微调，标注君臣佐使
输出：{"base_formula":"清肺散","herbs":[{name,dosage_g,role}...],"usage":"30-50g/头拌料","references":[...]}
```

**Step 4 校验Agent**（`safety.py` 规则引擎，非LLM，保证用药安全）
```
① 十八反查表：甘草+甘遂？→ 拦截        （硬规则）
② 幻觉剂量：麻黄500g（超上限8倍）→ 拦截 （硬规则）
③ 超人用区间 → 软提示（兽用整方给药属正常）
④ 孕猪慎用药 → 软提示
通过 → 输出报告；拦截 → 标记人工审核
```

### 3.4 输出层

**诊疗报告 JSON 结构**：
```json
{
  "syndrome": {"stage": "中期", "syndrome": "疫热壅肺", "principle": "清热宣肺，化痰平喘",
               "evidence": [...], "references": ["【来源：辨证库】..."]},
  "prescription": {"base_formula": "清肺散", "herbs": [{"name":"板蓝根","dosage_g":30,"role":"君"}...],
                   "usage": "30-50g/头拌料", "course": "连用3-5天",
                   "contraindications": [...], "references": [...]},
  "verification": {"safe": true, "errors": [], "warnings": [...]}
}
```

**去向**：① 前端展示 ② 落库（见第四节）③ 供疗效追踪

### 3.5 反馈闭环

```
用药第3/7天 → 记录咳嗽频率/体温/精神变化（treatment_feedback）
→ 治愈案例：强化论治库对应方剂权重
→ 无效案例：兽医复盘，修正 add_rule 或辨证规则
```

---

## 四、代码文件与职责对照

| 文件 | 职责 | 运行方式 |
|------|------|---------|
| `config.py` | 环境配置统一读取（.env：AI模型+数据库） | 被所有模块引用 |
| `test_qwen.py` | 网关连通性测试（qwen3.7-plus） | 直接运行 |
| `rag_retrieve.py` | 三库内存检索（jieba哈希向量+余弦） | 被 therapy_engine 调用 |
| `safety.py` | 校验Agent：剂量换算+十八反+幻觉拦截 | 被 therapy_engine 调用 |
| `therapy_engine.py` | 论治引擎：查询改写→辨证Agent→组方Agent→校验Agent | 被 API/交互终端调用 |
| `api_server.py` | FastAPI 服务（POST /api/diagnosis，含落库） | uvicorn 启动 |
| `interactive_demo.py` | 交互式手动诊断终端 | 直接运行 |
| `test_manual.py` | 自动手动测试（6块） | 直接运行 |
| `db_schema.sql` | 建库+种子数据 | mysql 执行一次 |
| `db_seed_extend.sql` | 扩充数据（爬取资料入库） | mysql 执行一次 |

**调用链**：
```
interactive_demo / api_server / test_manual
        └──▶ therapy_engine.treatment_pipeline()
                  ├──▶ rag_retrieve.retrieve()      # 三库检索
                  ├──▶ call_qwen()                   # 辨证Agent / 组方Agent（LLM）
                  └──▶ safety.safety_check()         # 校验Agent（规则）
        └──▶ pymysql                                  # 落库（api_server 内）
```

---

## 四·五、引用来源显示规范（为什么引用不是数据库表名）

**知识分层存储**（两套底座，各有分工）：

| 知识形态 | 存储位置 | 引用显示 |
|---------|---------|---------|
| 结构化知识（辨证规则/方剂/药材/禁忌） | MySQL `pig_diag`（权威源）→ 导出 CSV → 内存索引 | 【来源：辨证库】/【来源：论治库】 |
| 典籍全文语料（教材摘录/诊疗规范） | 本地文件 `data/rag/classics/*.txt` → 内存索引 | 【来源：典籍·文件名】（如"典籍·中兽医学_三期辨证"） |

- **为什么典籍不入 MySQL**：长文本全文用文件+语义索引是 RAG 标准做法，检索更快、编辑方便、不占关系库；MySQL 负责需要精确查询/校验/审计的结构化数据
- **为什么引用显示"典籍·文件名"而非完整路径**：展示层给兽医/评审看的是友好名称；完整文件路径由 `rag_retrieve.py` 索引内部持有，供技术审计追溯
- **防幻觉约束**：LLM 只能引用注入片段中的【来源】标签，无依据必须标注"缺检索依据"，禁止自造出处

## 五、数据库读写流转

| 环节 | 读 | 写 |
|------|----|----|
| RAG 检索 | `data/kg/*.csv`（MySQL 导出）+ `data/rag/classics/*.txt` | — |
| 辨证 | 辨证库/典籍库检索片段 | — |
| 组方 | 论治库检索片段 | — |
| 校验 | `herb`（剂量区间）、`herb_contraindication`（禁忌） | — |
| 落库 | — | `diagnosis_record`（辨病）、`prescription_record`（组方+校验） |
| 反馈 | `prescription_record` | `treatment_feedback`（疗效） |
| 审计 | `kg_source`（所有知识出处） | — |

> MySQL 是**权威源**；`data/kg/*.csv` 是其导出（供 RAG 检索）。数据更新流程：改 MySQL → 重导 CSV → 重建检索索引（重启进程即可）。

---

## 六、启动与部署

### 6.1 本地演示（三步）

```bash
# 1. 数据库（已建好；重建时执行）
mysql -u root -p < db_schema.sql
mysql -u root -p < db_seed_extend.sql

# 2. API 服务（可选，前端对接用）
python -m uvicorn api_server:app --host 0.0.0.0 --port 8000
#   文档: http://localhost:8000/docs

# 3. 三个入口任选
python interactive_demo.py     # 交互式手动诊断（推荐演示）
python test_manual.py all      # 全量自测
python test_manual.py cases    # 只跑端到端案例
```

### 6.2 与整合板块/前端对接

- **接口**：`POST http://<服务器>:8000/api/diagnosis`
- **请求**：`{"disease":"猪支原体肺炎","confidence":0.91,"symptoms":"湿咳...","temp_c":39.2,"weight_kg":70,"severity":"中度","pig_house":"第8圈第6舍"}`
- **响应**：`{"code":0,"trace_id":"RX-1","syndrome":{...},"prescription":{...},"verification":{...}}`
- **前端页面建议**：证型卡片（分期/证型/治则）+ 组方表格（药材/剂量/君臣佐使）+ 校验状态（✅/⚠️）+ 引用来源折叠区

---

## 七、端到端演示流程（照着做）

```
1. 启动 MySQL（本地已运行）
2. 启动 API：python -m uvicorn api_server:app --port 8000
3. 打开 http://localhost:8000/docs  → 点 /api/diagnosis → Try it out
4. 填入案例A数据：
   {"disease":"猪支原体肺炎","symptoms":"湿咳、痰多、呼吸粗、食欲下降",
    "temp_c":39.2,"weight_kg":70,"severity":"中度","pig_house":"第8圈第6舍"}
5. Execute → 看到：证型=疫热壅肺、组方=清肺散、校验=通过、引用=论治库来源
6. 演示前端：interactive_demo.py 逐项输入（或直接回车看示例）
7. 验证落库：mysql -u root -p pig_diag -e "SELECT * FROM prescription_record ORDER BY id DESC LIMIT 1"
```

---

## 八、故障排查速查

| 现象 | 原因 | 处理 |
|------|------|------|
| `InvalidApiKey` | .env 密钥错 | 检查 DASHSCOPE_API_KEY |
| LLM 输出非 JSON | 温度高/提示词被改 | 检查 therapy_engine prompt，temperature 保持 0.2/0.5 |
| 检索不到相关片段 | CSV 未更新/语料缺失 | 重导 CSV、重启进程重建索引 |
| 校验误拦兽用方 | 人用区间 vs 兽用整方 | 属正常软提示；硬拦截仅 8 倍上限+十八反 |
| API 400 | 请求体缺字段 | 对照 6.2 请求结构 |
| 落库失败 | 外键/字段类型 | 看报错，证候名需存在于 syndrome 表 |

---

## 附录：运行流程图（ASCII 时序）

```
用户/前端            API服务          论治引擎            三库/DB           LLM(qwen)
   │   POST /api/diagnosis  │                │                 │              │
   ├───────────────────────▶│                │                 │              │
   │                        │  辨病结果传递    │                 │              │
   │                        ├───────────────▶│                 │              │
   │                        │                │ ①RAG检索辨证库/典籍库             │
   │                        │                ├────────────────▶               │
   │                        │                │ ②辨证Agent prompt+片段           │
   │                        │                ├───────────────────────────────▶│
   │                        │                │◀──────── 证型JSON ────────────│
   │                        │                │ ③RAG检索论治库+典籍库             │
   │                        │                ├────────────────▶               │
   │                        │                │ ④组方Agent prompt+基础方          │
   │                        │                ├───────────────────────────────▶│
   │                        │                │◀──────── 处方JSON ────────────│
   │                        │                │ ⑤校验Agent(规则)                 │
   │                        │                │ ⑥落库 diagnosis/prescription    │
   │                        │◀─────────── 完整诊疗报告 ───────│                 │
   │◀───────────────────────│                │                 │              │
   │  展示: 证型/组方/校验/引用 │                │                 │              │
```

> 一次完整诊疗耗时：约 5-15 秒（2 次 LLM 调用为主），检索/校验毫秒级。
