# 诊疗决策板块 — 数据库设计

> **存储方案**：MySQL 8.0（结构化业务数据）+ ChromaDB（向量检索语料）  
> **对齐**：《探讨预期.docx》辨证库/论治库/理-法-方-药逻辑链 + 技术路线报告  
> **用途**：辨病模型结果落库、RAG 论治检索、闭环反馈追踪

---

## 一、总体设计

### 1.1 存储分工

| 存储 | 用途 | 说明 |
|------|------|------|
| **MySQL** | 结构化知识（辨证库/论治库/药材/禁忌）+ 业务记录（诊断/处方/反馈） | 事实权威源，支持 Text-to-SQL 精确查询 |
| **ChromaDB** | 典籍语料向量 + 双库向量化副本 | RAG 语义检索（构建脚本见实操手册6.2） |
| **JSON/CSV** | 离线种子数据、导出备份 | 与 MySQL 可互导 |

> 关系：**MySQL 是权威源**，ChromaDB 是 MySQL 的向量化投影。RAG 检索命中后回 MySQL 取完整记录。

### 1.2 ER 关系总览（理-法-方-药逻辑链落库）

```
┌────────────┐    判定      ┌────────────┐   适用     ┌────────────┐
│ syndrome_  │────────────▶ │ syndrome   │──────────▶ │ formula    │
│ mapping    │ 体征组合→证候  │ (证候/治则) │ 证候→方剂  │ (方剂)     │
│ (辨证库)   │              └────────────┘            └─────┬──────┘
└────────────┘                                             │ 组成
                                                           ▼
                                 ┌────────────┐    关联    ┌────────────┐
                                 │ formula_   │──────────▶ │ herb       │
                                 │ herb       │ 方剂-药材   │ (药材)     │
                                 │ (组成+剂量) │           └─────┬──────┘
                                 └────────────┘                 │ 禁忌
                                                                ▼
                                   ┌────────────────────────┐
                                   │ herb_contraindication  │
                                   │ (十八反十九畏/禁忌)     │
                                   └────────────────────────┘

业务链（闭环反馈）：
  diagnosis_record ──▶ prescription_record ──▶ treatment_feedback
  (辨病结果)             (组方+校验结果)           (疗效追踪)
```

### 1.3 命名规范

- 表名：小写蛇形（`syndrome_mapping`）
- 字段：小写蛇形（`cough_type`）
- 主键：`id BIGINT AUTO_INCREMENT`
- 外键：`xxx_id`
- 时间：`created_at DATETIME DEFAULT CURRENT_TIMESTAMP`
- 字符集：`utf8mb4`（支持中文/emoji）
- 所有知识表含 `reference_id`（引用来源，杜绝幻觉出处）

---

## 二、知识库表设计（论治层数据底座）

### 2.1 `syndrome` 证候表

标准化证候定义（治则治法挂此表），中医三期为顶层分类。

```sql
CREATE TABLE syndrome (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name          VARCHAR(64)  NOT NULL UNIQUE COMMENT '证候名，如：疫热壅肺',
  stage         VARCHAR(32)  NOT NULL COMMENT '中医分期：前期(邪袭肺卫)/中期(疫热壅肺)/后期(气阴两伤)',
  description   VARCHAR(512) COMMENT '证候描述',
  principle     VARCHAR(128) NOT NULL COMMENT '治则治法，如：清热化痰、宣肺平喘',
  reference_id  BIGINT UNSIGNED COMMENT '知识来源ID',
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='证候表';
```

### 2.2 `syndrome_mapping` 辨证库（多维体征 → 证候）

探讨预期的**辨证库**：多维体征映射标准化证候。每条规则 = 一组体征组合 → 一个证候。

```sql
CREATE TABLE syndrome_mapping (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  temperature_min DECIMAL(4,1) COMMENT '体温下限(℃)，NULL=不限',
  temperature_max DECIMAL(4,1) COMMENT '体温上限(℃)，NULL=不限',
  cough_type    VARCHAR(128) COMMENT '咳喘表现关键词，如：湿咳、痰多、干咳',
  excretion     VARCHAR(128) COMMENT '排泄物观察，如：正常/便秘/稀溏',
  other_signs   VARCHAR(256) COMMENT '其他体征：精神、采食、呼吸等',
  syndrome_id   BIGINT UNSIGNED NOT NULL COMMENT '命中证候',
  disease_id    BIGINT UNSIGNED COMMENT '关联疾病（可空）',
  evidence      VARCHAR(512) COMMENT '鉴别依据（专家标注）',
  weight        TINYINT UNSIGNED DEFAULT 5 COMMENT '权重1-10，同体征多规则时优先',
  reference_id  BIGINT UNSIGNED,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY idx_syndrome (syndrome_id),
  KEY idx_disease (disease_id),
  KEY idx_temp (temperature_min, temperature_max)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='辨证库：多维体征→证候映射规则';
```

### 2.3 `disease` 疾病表（含别名列）

```sql
CREATE TABLE disease (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name          VARCHAR(64)  NOT NULL UNIQUE COMMENT '疾病名，如：猪支原体肺炎',
  alias         VARCHAR(256) COMMENT '别名，逗号分隔（俗名/缩写/曾用名），如：蓝耳病,PRRS',
  pathogen      VARCHAR(128) COMMENT '病原体',
  epidemiology  VARCHAR(512) COMMENT '流行病学特点',
  symptoms      VARCHAR(512) COMMENT '典型症状',
  label         VARCHAR(64)  COMMENT '辨病模型分类标签(英文小写)，如：mycoplasma',
  reference_id  BIGINT UNSIGNED,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='疾病表';
```

> **别名机制**（2026-08-09 新增）：`alias` 列存俗名/缩写（蓝耳病≡猪繁殖与呼吸综合征、锁喉风≡猪肺疫、猪气喘病≡猪支原体肺炎）。`kb_lookup._same_disease()` 做别名归一比较，`rule_cross_check` 据此判断上游辨病与规则映射疾病是否同一——避免"猪蓝耳病"vs"猪繁殖与呼吸综合征"字面不同导致的误报矛盾。

### 2.4 `formula` 方剂表

```sql
CREATE TABLE formula (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name          VARCHAR(64)  NOT NULL UNIQUE COMMENT '方剂名，如：麻杏石甘汤加减',
  syndrome_id   BIGINT UNSIGNED NOT NULL COMMENT '主治证候',
  usage_method  VARCHAR(256) COMMENT '用法，如：按500g饲料混饲',
  course        VARCHAR(64)  COMMENT '疗程，如：连用5-7天',
  notes         VARCHAR(512) COMMENT '方解/备注',
  reference_id  BIGINT UNSIGNED,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY idx_syndrome (syndrome_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='方剂表';
```

### 2.5 `formula_herb` 方剂-药材组成表

```sql
CREATE TABLE formula_herb (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  formula_id    BIGINT UNSIGNED NOT NULL,
  herb_id       BIGINT UNSIGNED NOT NULL,
  dosage_g      DECIMAL(6,1) NOT NULL COMMENT '基础剂量(克)',
  role          VARCHAR(16)  NOT NULL COMMENT '君臣佐使',
  note          VARCHAR(256) COMMENT '该药在方中作用说明',
  sort_order    TINYINT UNSIGNED DEFAULT 0 COMMENT '排列顺序',
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_formula_herb (formula_id, herb_id),
  KEY idx_herb (herb_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='方剂组成表';
```

### 2.6 `add_rule` 加减化裁规则表

```sql
CREATE TABLE add_rule (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  formula_id    BIGINT UNSIGNED NOT NULL COMMENT '所属方剂',
  condition_desc VARCHAR(256) NOT NULL COMMENT '加减条件，如：痰多加',
  herb_id       BIGINT UNSIGNED COMMENT '加减药物(可空=减药)',
  operation     VARCHAR(8)   NOT NULL DEFAULT '加' COMMENT '加/减/调整剂量',
  dosage_g      DECIMAL(6,1) COMMENT '加减剂量',
  reference_id  BIGINT UNSIGNED,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY idx_formula (formula_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='随症加减规则表';
```

### 2.7 `herb` 药材表

```sql
CREATE TABLE herb (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name          VARCHAR(64)  NOT NULL UNIQUE COMMENT '药材名',
  property      VARCHAR(16)  COMMENT '四性：寒/热/温/凉/平',
  flavor        VARCHAR(32)  COMMENT '五味：辛甘酸苦咸',
  meridian      VARCHAR(64)  COMMENT '归经：肺/脾/胃等',
  effect        VARCHAR(256) COMMENT '功效',
  dosage_min    DECIMAL(6,1) COMMENT '安全剂量下限(克)',
  dosage_max    DECIMAL(6,1) COMMENT '安全剂量上限(克)',
  caution       VARCHAR(256) COMMENT '使用注意（孕猪禁用等）',
  reference_id  BIGINT UNSIGNED,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='药材表';
```

### 2.8 `herb_contraindication` 配伍禁忌表（安全护栏）

```sql
CREATE TABLE herb_contraindication (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  herb_a        BIGINT UNSIGNED NOT NULL COMMENT '药材A',
  herb_b        BIGINT UNSIGNED NOT NULL COMMENT '药材B',
  rule_type     VARCHAR(16)  NOT NULL COMMENT '十八反/十九畏/孕猪禁用/其他',
  description   VARCHAR(256) COMMENT '说明',
  reference_id  BIGINT UNSIGNED,
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_pair (herb_a, herb_b)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配伍禁忌表(安全护栏)';
```

### 2.9 `kg_source` 知识来源表（杜绝幻觉引用）

```sql
CREATE TABLE kg_source (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  title         VARCHAR(256) NOT NULL COMMENT '文献/出处标题',
  category      VARCHAR(32)  COMMENT '类型：教材/典籍/临床病例/PPT验证',
  detail        VARCHAR(512) COMMENT '章节/页码等定位信息',
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识来源表';
```

---

## 三、业务记录表设计（闭环反馈）

### 3.1 `diagnosis_record` 辨病记录

```sql
CREATE TABLE diagnosis_record (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  pig_house     VARCHAR(64)  COMMENT '猪舍编号，如：第8圈第6舍',
  model_label   VARCHAR(64)  COMMENT '辨病模型输出标签',
  disease_id    BIGINT UNSIGNED COMMENT '命中疾病',
  confidence    DECIMAL(4,3) COMMENT '置信度0-1',
  temp_c        DECIMAL(4,1) COMMENT '体温℃',
  mental_state  VARCHAR(64)  COMMENT '精神状态',
  env_json      JSON         COMMENT '环境参数快照',
  feature_json  JSON         COMMENT '声学特征快照(可选)',
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY idx_house_time (pig_house, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='辨病记录';
```

### 3.2 `prescription_record` 处方记录（组方 + 校验结果）

```sql
CREATE TABLE prescription_record (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  diagnosis_id    BIGINT UNSIGNED COMMENT '关联辨病记录',
  syndrome_id     BIGINT UNSIGNED COMMENT '辨证结果-证候',
  formula_id      BIGINT UNSIGNED COMMENT '基础方剂',
  herbs_json      JSON         COMMENT '组方明细[{name, dosage_g, role}]',
  usage_method    VARCHAR(256) COMMENT '用法',
  course          VARCHAR(64)  COMMENT '疗程',
  safety_approved TINYINT(1)   DEFAULT 0 COMMENT '校验是否通过',
  safety_report   JSON         COMMENT '校验报告{warnings, issues, score}',
  llm_raw         TEXT         COMMENT 'LLM原始输出(审计用)',
  references_json JSON         COMMENT '引用出处列表',
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY idx_diagnosis (diagnosis_id),
  KEY idx_syndrome (syndrome_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='处方记录';
```

### 3.3 `treatment_feedback` 疗效反馈表（闭环）

```sql
CREATE TABLE treatment_feedback (
  id                BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  prescription_id   BIGINT UNSIGNED NOT NULL COMMENT '关联处方',
  day_after         TINYINT UNSIGNED COMMENT '用药第N天',
  cough_freq_change VARCHAR(32)  COMMENT '咳嗽频率变化，如：-40%',
  temp_change       VARCHAR(32)  COMMENT '体温变化，如：-0.8℃',
  mental_change     VARCHAR(64)  COMMENT '精神状态变化',
  outcome           VARCHAR(32)  COMMENT '结局：治愈/好转/无效/恶化',
  adjustment        VARCHAR(256) COMMENT '方案调整',
  effective_score   DECIMAL(3,2) COMMENT '疗效评分0-1',
  created_at        DATETIME DEFAULT CURRENT_TIMESTAMP,
  KEY idx_prescription (prescription_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='疗效反馈表';
```

---

## 四、数据字典速查

| 表 | 职责 | 对应探讨预期 |
|----|------|-------------|
| syndrome | 证候/治则定义 | 标准化"证候" |
| syndrome_mapping | 多维体征→证候规则 | **辨证库** |
| disease | 疾病主数据 | 辨病标签落库 |
| formula | 方剂主数据 | **论治库**基础方 |
| formula_herb | 方剂-药材组成+剂量 | 论治库方剂明细 |
| add_rule | 随症加减规则 | **理-法-方-药链**加减环节 |
| herb | 药材属性+安全剂量区间 | 药物属性与禁忌 |
| herb_contraindication | 十八反十九畏 | **安全护栏** |
| kg_source | 知识出处 | 可追溯/防幻觉 |
| diagnosis_record | 辨病输出 | 工作流第2步 |
| prescription_record | 组方+校验输出 | 工作流第4-5步 |
| treatment_feedback | 疗效追踪 | **闭环反馈** |

**理-法-方-药链落库路径**：

```
syndrome_mapping(多维体征) → syndrome(证候=理+法) → formula(方) → formula_herb(药+剂量)
                                                    └─ add_rule(随症加减) → herb(药物属性)
```

---

## 五、检索对接说明

| 场景 | 查询方式 | 说明 |
|------|---------|------|
| RAG 辨证检索 | ChromaDB 向量 → 回表 `syndrome_mapping` 取完整规则 | 语义匹配体征描述 |
| Text-to-SQL 精确检索 | LLM 生成 SQL → MySQL | 条件明确（如体温区间），命中 `syndrome_mapping` |
| 组方检索 | `formula` JOIN `formula_herb` JOIN `herb` | 按证候取方剂+剂量+安全区间 |
| 安全校验 | `herb` 剂量区间 + `herb_contraindication` 查表 | 确定性规则，非LLM |
| 疗效分析 | `treatment_feedback` 聚合 | 按处方/证候统计有效率 |
| 业务报表 | `diagnosis_record` + `prescription_record` | 前端小程序展示 |

> 建库脚本：见 `db_schema.sql`（整合版：建库 + 种子 + 网络资料扩充 + 蓝耳病/别名，一条命令全量执行）。数据录入顺序：`kg_source` → `disease` → `syndrome` → `syndrome_mapping` → `herb` → `formula` → `formula_herb` → `add_rule` → `herb_contraindication`。

**当前数据规模（2026-08-09，已入库）**：13 来源 / **6 疾病**（含猪繁殖与呼吸综合征/蓝耳病）/ 7 证候 / **11 辨证规则**（含蓝耳病急性/慢性 2 条）/ 48 药材 / 14 方剂 / 73 组成 / 11 加减规则 / 8 配伍禁忌。扩充来源：网络爬取资料（`data/kg/*.md`，全部带来源 URL）+ PPT 已验证方剂 + 典籍蓝耳病章节。新增数据流程：改 MySQL → 重导 CSV（`data/kg/*.csv`）→ 重启进程重建检索索引。建库脚本：`db_schema.sql` 一个文件全量（2026-08-13 已整合，历史分片脚本已删除）。
