 

# 诊疗决策板块 — 实操手册

> 配套文档：`诊疗决策板块_技术路线报告.md`
> 分工：阴学舟（主责·模型+RAG论治引擎）、刘宇翔（诊断模型+校验）、柴堰颖（前端+三库数据）
> 原则：**先有再完善**，每步都有验证点，卡住先看第8节排错

---

## 目录

1. [环境搭建](#1-环境搭建)
2. [数据准备](#2-数据准备)
3. [诊断模型实操](#3-诊断模型实操)
4. [辨证组方引擎实操（RAG论治）](#4-辨证组方引擎实操认知层论治)
5. [三大知识库建设](#5-三大知识库建设论治层核心工作量)
6. [RAG 知识库实操](#6-rag-知识库实操)
7. [集成联调与验收](#7-集成联调与验收)
8. [常见问题排查](#8-常见问题排查)
9. [进度自查表](#9-进度自查表)

---

## 1. 环境搭建

### 1.1 本机环境（Windows / WSL2 均可）

```bash
# Python 3.10+ 检查
python --version

# 创建虚拟环境（建议）
python -m venv venv_diag
# Windows:
venv_diag\Scripts\activate
# WSL/Linux:
source venv_diag/bin/activate
```

### 1.2 安装依赖

```bash
pip install -U pip
pip install numpy pandas scikit-learn librosa soundfile
pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121   # GPU版
# 无GPU则: pip install torch torchaudio
pip install dashscope chromadb langchain langchain-openai neo4j py2neo
pip install python-dotenv   # .env 密钥加载必备
pip install pydub jieba requests
```

> 验证点：`python -c "import torch, librosa, dashscope; print('OK')"` 无报错。
> GPU 可用性：`python -c "import torch; print(torch.cuda.is_available())"` 输出 True 最佳，False 也能跑（慢）。

### 1.3 阿里云百炼平台开通（硬性要求）

1. 打开  https://bailian.console.aliyun.com/ ，用阿里云账号登录
2. 首次使用需**开通百炼服务**（免费额度，学生/赛事有300元算力补贴）
3. 进入 **API-KEY 管理** → 创建 API-KEY → 复制保存（格式 `sk-xxxx`）
4. 创建密钥后截图保存（项目要求提供调用凭证/截图）

### 1.4 环境配置（.env + config.py 统一读取）

项目根目录创建 `.env` 文件（**不要提交到 git**）：

```bash
# AI 模型（百炼自定义网关，OpenAI 兼容）
DASHSCOPE_API_KEY=sk-你的密钥
DASHSCOPE_BASE_URL=https://ws-你的专属网关.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
DASHSCOPE_MODEL=qwen3.7-plus

# 数据库（MySQL）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=你的密码
DB_NAME=pig_diag
```

代码中统一通过 `config.py` 读取（**禁止在业务代码里硬编码**）：

```python
from config import LLM_API_KEY, LLM_BASE_URL, LLM_MODEL, DB, check_env
# LLM_API_KEY / LLM_BASE_URL / LLM_MODEL → AI 模型配置
# DB → MySQL 连接参数（pymysql.connect(**DB)）
# check_env() → 环境自检，返回缺失项列表
```

> 换模型/网关/服务器只改 `.env` 一处；`.env.example` 模板可复制给团队。

### 1.5 快速测试 Qwen 调用

**方式A：默认网关（模型名 `qwen-max`/`qwen-plus`/`qwen-turbo`）**

```python
# test_qwen.py
import os
from dotenv import load_dotenv

load_dotenv()

api_key = os.environ.get("DASHSCOPE_API_KEY", "")
if not api_key:
    print("❌ 未找到 DASHSCOPE_API_KEY")
    print("   1. 检查项目根目录 .env 文件是否存在")
    print("   2. 确认内容格式: DASHSCOPE_API_KEY=sk-xxx")
    exit(1)

from dashscope import Generation

resp = Generation.call(
    model="qwen-plus",          # 有效模型名: qwen-max / qwen-plus / qwen-turbo
    messages=[{"role": "user", "content": "猪咳嗽三天，体温39.8℃，痰多，请用一句话给个初步判断"}],
    result_format="message",
)

if resp.status_code == 200:
    print(resp.output.choices[0].message.content)
else:
    print("❌ 调用失败，诊断信息：")
    print("  status_code:", resp.status_code)
    print("  code:", resp.code)
    print("  message:", resp.message)
```

> ⚠️ `dashscope.Generation` 只走官方网关，**无法访问专属自定义网关**。用自定义网关必须用方式B。

**方式B：自定义网关（专属模型，如赛事环境）**

专属服务地址在百炼控制台 → **模型服务 → 专属服务** 获取，格式 `https://ws-xxx.region.maas.aliyuncs.com/api/v1`，**OpenAI 兼容**：

```python
# test_qwen.py — OpenAI兼容模式
import os
from dotenv import load_dotenv

load_dotenv()

api_key = os.environ.get("DASHSCOPE_API_KEY", "")
if not api_key:
    print("❌ 未找到 DASHSCOPE_API_KEY")
    exit(1)

BASE_URL = "https://ws-你的专属网关.cn-beijing.maas.aliyuncs.com/api/v1"
MODEL = "你的专属模型名"   # 如 qwen3.7-plus

from openai import OpenAI

client = OpenAI(api_key=api_key, base_url=BASE_URL)

try:
    resp = client.chat.completions.create(
        model=MODEL,
        messages=[{"role": "user", "content": "猪咳嗽三天，体温39.8℃，痰多，请用一句话给个初步判断"}],
    )
    print(resp.choices[0].message.content)
except Exception as e:
    print("❌ 调用失败：")
    print("  HTTP", getattr(e, "status_code", "?"))
    print("  body:", getattr(e, "body", str(e)))
```

> 需要 `pip install openai`。后续所有 LLM 调用（4.1节 therapy_engine.py）都要用这个 OpenAI 兼容方式，替换 `dashscope.Generation`。
> 验证点：能返回中文回答。

---

## 2. 数据准备

### 2.1 数据目录结构（统一规范）

```
data/
├── audio/
│   ├── cough/            # 咳嗽声样本 (wav)
│   ├── non_cough/        # 非咳嗽声 (尖叫/呼噜/哼哼/环境音)
│   └── disease/          # ★按疾病分类的样本（诊疗决策核心）
│       ├── mycoplasma/   # 支原体肺炎
│       ├── pasteurella/  # 猪肺疫
│       ├── influenza/    # 猪流感
│       └── ...           # 按实际标注扩展
├── labels/
│   ├── cough_labels.csv       # 咳嗽样本标注
│   └── diagnosis_labels.csv   # 疾病诊断标注（含兽医审核列）
└── kg/                        # 三库数据（症状-疾病/疾病-处方/药材表）
    ├── herbs.csv              # 药材
    ├── formulas.csv           # 方剂
    └── relations.csv          # 关系
```

### 2.2 诊断标注表格式（diagnosis_labels.csv）

```csv
file_id,path,label,disease,cough_type,severity,vet_1,vet_2,status
D001,data/audio/disease/mycoplasma/D001.wav,mycoplasma,支原体肺炎,湿咳,中度,同意,同意,confirmed
D002,data/audio/disease/influenza/D002.wav,influenza,猪流感,干咳,轻度,同意,待审,pending
```

- `label`：模型分类标签（英文小写）
- `vet_1/vet_2`：两位兽医独立审核意见，双同意才进训练集
- 金标准确诊样本（有病原检测确认的）占总样本 5%~20% 即可

### 2.3 音频预处理脚本

```python
# preprocess.py — 统一采样率/时长/预加重
import librosa
import numpy as np
import os

SR = 16000          # 统一采样率
TARGET_LEN = 1.0    # 统一时长（秒）

def preprocess_audio(path, out_dir):
    y, sr = librosa.load(path, sr=SR)
    # 裁剪或填充到1秒
    target = int(SR * TARGET_LEN)
    if len(y) > target:
        y = y[:target]
    else:
        y = np.pad(y, (0, target - len(y)))
    # 预加重 (系数0.97)
    y = np.append(y[0], y[1:] - 0.97 * y[:-1])
    out = os.path.join(out_dir, os.path.basename(path))
    librosa.output.write_wav(out, y, SR)   # 新版用 soundfile
    return out

# 批量处理
for split in ["train", "val", "test"]:
    os.makedirs(f"data/processed/{split}", exist_ok=True)
    for cls in os.listdir(f"data/audio/{split}"):
        cls_dir = f"data/audio/{split}/{cls}"
        if not os.path.isdir(cls_dir):
            continue
        os.makedirs(f"data/processed/{split}/{cls}", exist_ok=True)
        for f in os.listdir(cls_dir):
            if f.endswith(".wav"):
                preprocess_audio(os.path.join(cls_dir, f),
                                 f"data/processed/{split}/{cls}")
```

### 2.4 特征提取脚本

```python
# features.py — 提取MFCC + logFBank + 时频统计特征
import librosa
import numpy as np
import pandas as pd

SR = 16000

def extract_features(path):
    y, _ = librosa.load(path, sr=SR)
    # MFCC 13维 + 一阶二阶差分 = 39维
    mfcc = librosa.feature.mfcc(y=y, sr=SR, n_mfcc=13, n_fft=512, hop_length=160)
    delta = librosa.feature.delta(mfcc)
    delta2 = librosa.feature.delta(mfcc, order=2)
    feat_mfcc = np.vstack([mfcc, delta, delta2]).T   # (T, 39)
    # logFBank 64维
    fbank = librosa.feature.melspectrogram(y=y, sr=SR, n_mels=64, n_fft=512, hop_length=160)
    feat_fbank = librosa.logamplitude(fbank).T       # (T, 64)
    # 统计特征：均值/方差压缩
    mfcc_mean = feat_mfcc.mean(axis=0)               # (39,)
    mfcc_std  = feat_mfcc.std(axis=0)                # (39,)
    fbank_mean = feat_fbank.mean(axis=0)             # (64,)
    # 拼接成单向量
    vec = np.concatenate([mfcc_mean, mfcc_std, fbank_mean])  # (142,)
    return vec

# 全量提取并保存
def build_feature_csv(split="train"):
    rows, labels = [], []
    for cls in os.listdir(f"data/processed/{split}"):
        cls_dir = f"data/processed/{split}/{cls}"
        for f in os.listdir(cls_dir):
            if f.endswith(".wav"):
                rows.append(extract_features(os.path.join(cls_dir, f)))
                labels.append(cls)
    X = np.array(rows)
    y = np.array(labels)
    np.savez(f"data/features/{split}.npz", X=X, y=y)
    print(f"{split}: {X.shape}, 类别: {np.unique(y, return_counts=True)}")

for s in ["train", "val", "test"]:
    build_feature_csv(s)
```

---

## 3. 诊断模型实操

### 3.1 Step 1：Baseline — SVM 多分类（先跑通）

```python
# train_svm_baseline.py
import numpy as np
from sklearn.svm import SVC
from sklearn.metrics import classification_report, accuracy_score

train = np.load("data/features/train.npz")
val   = np.load("data/features/val.npz")
X_tr, y_tr = train["X"], train["y"]
X_va, y_va = val["X"], val["y"]

model = SVC(kernel="rbf", C=100, gamma=0.01, probability=True)
model.fit(X_tr, y_tr)
pred = model.predict(X_va)
print("准确率:", accuracy_score(y_va, pred))
print(classification_report(y_va, pred))
```

> 验证点：多分类准确率 ≥80% 说明特征有效，继续优化；<70% 先查数据问题。

### 3.2 Step 2：CNN-BiLSTM 多分类（主模型）

```python
# train_cnn_bilstm.py
import torch
import torch.nn as nn
import numpy as np
from sklearn.preprocessing import LabelEncoder

class CNNBiLSTM(nn.Module):
    def __init__(self, n_classes, input_channels=1):
        super().__init__()
        self.conv = nn.Sequential(
            nn.Conv2d(input_channels, 32, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 3, padding=1), nn.ReLU(), nn.MaxPool2d(2),
        )
        self.lstm = nn.LSTM(64 * 7 * 2, 64, bidirectional=True, batch_first=True)
        self.fc = nn.Sequential(
            nn.Linear(128, 64), nn.ReLU(), nn.Dropout(0.5),
            nn.Linear(64, n_classes),
        )
    def forward(self, x):
        # x: (B, C, T, F)
        b, c, t, f = x.shape
        h = self.conv(x)                    # (B, 64, T/4, F/4)
        h = h.permute(0, 2, 1, 3).reshape(b, t//4, -1)  # (B, T/4, 64*F/4)
        h, _ = self.lstm(h)                 # (B, T/4, 128)
        return self.fc(h.mean(dim=1))       # 时序均值池化

def load_data():
    tr, va = np.load("data/features/train.npz"), np.load("data/features/val.npz")
    # 将MFCC特征重排为图像形式 (B, 1, T, F)
    X_tr = tr["X"].reshape(-1, 1, 39, 8)[:, :, :, :]  # 具体reshape按实际特征图
    ...
```

> 实操提示：CNN-BiLSTM 的输入需要 **MFCC 特征图**（2D），不是扁平向量。若嫌麻烦，**先跑通扁平向量版**（直接 Dense+BiLSTM 处理 (T,39) 序列），准确率达标再上 CNN 卷积块。

### 3.3 Step 3：集成学习优化（SSLRB 简化版）

```python
# train_ensemble.py — SVM-Stacking + LR-Bagging 简化实现
from sklearn.svm import SVC
from sklearn.linear_model import LogisticRegression
from sklearn.model_selection import StratifiedKFold
import numpy as np

def svm_stacking(X, y, X_test, base_params_list, n_folds=3):
    """第一层: 多个SVM基分类器, 分层K折交叉验证生成元特征"""
    skf = StratifiedKFold(n_splits=n_folds, shuffle=True, random_state=42)
    meta_train = np.zeros((len(X), len(base_params_list)))
    meta_test = np.zeros((len(X_test), len(base_params_list)))

    for i, params in enumerate(base_params_list):
        for tr_idx, va_idx in skf.split(X, y):
            clf = SVC(kernel="rbf", probability=True, **params)
            clf.fit(X[tr_idx], y[tr_idx])
            meta_train[va_idx, i] = clf.predict_proba(X[va_idx])[:, 1]
        clf_full = SVC(kernel="rbf", probability=True, **params)
        clf_full.fit(X, y)
        meta_test[:, i] = clf_full.predict_proba(X_test)[:, 1]
    return meta_train, meta_test

# 使用示例
train = np.load("data/features/train.npz")
val   = np.load("data/features/val.npz")
X_tr, y_tr = train["X"], train["y"]
X_va, y_va = val["X"], val["y"]

base_params = [
    {"C": 100,  "gamma": 0.1},
    {"C": 100,  "gamma": 0.01},
    {"C": 100,  "gamma": 0.001},
]
meta_tr, meta_va = svm_stacking(X_tr, y_tr, X_va, base_params)

# 第二层: LR-Bagging (3个逻辑回归平均)
from sklearn.linear_model import LogisticRegression
preds = []
for seed in [1, 2, 3]:
    lr = LogisticRegression(max_iter=1000, random_state=seed)
    lr.fit(meta_tr, y_tr)
    preds.append(lr.predict_proba(meta_va)[:, 1])
final = np.mean(preds, axis=0)
pred = (final > 0.5).astype(int)
print("集成准确率:", accuracy_score(y_va, pred))
```

> 验证点：集成后准确率应比单 SVM baseline **高 2~5%**，尤其在类别不平衡时。**先保证二分类能跑通**，再扩展到多分类（多分类时二值化 One-vs-Rest 或改用元特征多类概率）。

### 3.4 模型评估报告

```python
# evaluate.py
from sklearn.metrics import classification_report, confusion_matrix, f1_score
import matplotlib.pyplot as plt
import seaborn as sns

# 评估并打印
report = classification_report(y_va, pred, target_names=class_names)
print(report)
print("Macro-F1:", f1_score(y_va, pred, average="macro"))

# 混淆矩阵热力图
cm = confusion_matrix(y_va, pred)
sns.heatmap(cm, annot=True, fmt="d", xticklabels=class_names, yticklabels=class_names)
plt.savefig("data/results/confusion_matrix.png", dpi=150)
```

> 必须保存输出：准确率、各类别P/R/F1、混淆矩阵图、训练曲线图。**技术文档要用**。

---

## 4. 辨证组方引擎实操（认知层·论治）

> 定调：**LLM + RAG，无需训练**。核心是三库（症状-疾病/疾病-处方/中医典籍）检索 + Qwen 两阶段生成（辨证→组方）+ 确定性规则校验。
> 📌 **代码已就绪**：完整实现见 `therapy_engine.py`（含下述全部增强），本节代码为最小可运行教学版。最新实现功能清单：
> - **查询改写层**（Step 0）：LLM 将口语输入规范化为检索词（失败自动回退）
> - **辨病纠错**：规则交叉验证（非LLM）+ LLM 独立判断 `disease_conflict`，矛盾时按症状辨证并警告
> - **SQL 多关键词检索**：多体征词 OR + evidence 字段匹配 + 同义词归一（犬类坐→犬坐/高温→高热）
> - **主症/特异体征优先**：犬坐/张口呼吸/血性泡沫 = 重症标志（疫热壅肺），权重高于泛化高热
> - **辨证温度 0.0**（确定性），组方 0.5（允许变化）

### 4.1 论治引擎核心代码（RAG 两阶段 + 规则校验）

```python
# therapy_engine.py — 论治引擎：RAG三库检索 + LLM两阶段辨证组方 + 规则校验
import os
import json
from dotenv import load_dotenv
load_dotenv()
from openai import OpenAI

api_key = os.environ["DASHSCOPE_API_KEY"]
# 用方式A默认网关时，BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"，MODEL = "qwen-plus"
BASE_URL = "https://ws-你的专属网关.cn-beijing.maas.aliyuncs.com/api/v1"
MODEL = "你的专属模型名"   # 如 qwen3.7-plus

def call_qwen(system_prompt, user_content, temperature=0.3):
    """OpenAI兼容调用（支持自定义专属网关）"""
    client = OpenAI(api_key=api_key, base_url=BASE_URL)
    resp = client.chat.completions.create(
        model=MODEL,
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_content},
        ],
        temperature=temperature,
    )
    return resp.choices[0].message.content

# ── 知识注入：分层策略（rag_retrieve.py 内存检索，零依赖）──
# 小库全量注入（FULL_KB=True 默认）：辨证库/论治库全给LLM做语义匹配，比弱向量检索准
# 大库检索：典籍库 Top-K；SQL 精确检索见 therapy_engine.sql_syndrome_lookup
# 说明：ChromaDB 写入可用，但查询在 Python 3.14 有 hnswlib 兼容问题，弃用。
from rag_retrieve import retrieve, get_all

FULL_KB = True  # False = 回退全检索模式（数据量变大时启用）

# ── 阶段一：辨证Agent（全量注入+典籍检索）────────────
SX_SYSTEM = """你是资深中兽医辨证专家，精通猪呼吸道疾病三期辨证论治
（前期邪袭肺卫→中期疫热壅肺→后期气阴两伤）。
你收到以下辨证库全部规则与典籍检索片段，必须基于它们辨证，不可虚构；
【主症优先】咳嗽类型/体温/呼吸为主症，精神/食欲/排泄物为兼症，
不得因单一兼症推翻主症指向的证候——需主症+多个兼症共同支持。
{sym_docs}
{med_docs}
输出严格JSON（无markdown包裹）：
{{
  "stage": "前期/中期/后期",
  "syndrome": "证型名称",
  "principle": "治则治法",
  "evidence": ["依据1", "依据2"],
  "references": ["引用出处（取自检索片段）"]
}}"""

def syndrome_stage(disease, symptoms, temp):
    """辨病结果 + 症状 → 证型/治则"""
    if FULL_KB:
        sym_docs = "\n".join(get_all('symptom_disease'))      # 9条全量
    else:
        sym_docs = "\n".join(retrieve('symptom_disease', f"{disease} {symptoms}", k=3))
    med_docs = "\n".join(retrieve('tcm_classics', f"{disease} {symptoms} 辨证 治则", k=5))
    user = f"辨病结果：{disease}\n临床症状：{symptoms}\n体温：{temp}℃\n请辨证分型。"
    return call_qwen(
        SX_SYSTEM.format(sym_docs=sym_docs, med_docs=med_docs),
        user, temperature=0.2,
    )

# ── 阶段二：组方Agent（全量注入+典籍检索）─────────────
RX_SYSTEM = """你是中兽药师、方剂学专家。
你收到以下论治库全部方剂与典籍检索片段，必须基于它们组方，不可虚构；
{sym_docs}
{formula_docs}
{med_docs}
结合猪只参数（体重/月龄/病情严重度）加减化裁，输出严格JSON（无markdown包裹）：
{{
  "base_formula": "基础方剂名",
  "herbs": [{{"name": "药材", "dosage_g": 15, "role": "君/臣/佐/使"}}],
  "add_notes": ["加减说明"],
  "preparation": "用法（如按500g饲料混饲）",
  "course": "疗程",
  "contraindications": ["禁忌"],
  "references": ["引用出处（取自检索片段）"]
}}"""

def formula_stage(sx_result, pig_info):
    """辨证结果 + 猪只信息 → 处方"""
    if FULL_KB:
        formula_docs = "\n".join(get_all('disease_formula'))   # 14首全量
    else:
        formula_docs = "\n".join(retrieve('disease_formula', f"{sx_result} 组方", k=3))
    med_docs = "\n".join(retrieve('tcm_classics', f"方剂 剂量 禁忌 加减", k=5))
    user = f"辨证结果：{sx_result}\n猪只信息：{pig_info}\n请开具组方。"
    return call_qwen(
        RX_SYSTEM.format(sym_docs="", formula_docs=formula_docs, med_docs=med_docs),
        user, temperature=0.5,
    )

# ── 阶段三：校验Agent（规则引擎，确定性规则，非LLM，见5.5节）─
def verify_stage(rx_text):
    """JSON解析 + 剂量区间 + 十八反十九畏检查"""
    rx = json.loads(rx_text)
    from safety import safety_check, calc_dosage
    report = safety_check(rx)
    return rx, report

# ── 主流水线 ─────────────────────────────────────────
def treatment_pipeline(disease, symptoms, temp, pig_info):
    print("[1/3] 辨证推理（RAG检索）...")
    sx = syndrome_stage(disease, symptoms, temp)
    print("[2/3] 组方生成（RAG检索）...")
    rx = formula_stage(sx, pig_info)
    print("[3/3] 规则校验...")
    try:
        rx_json, report = verify_stage(rx)
    except json.JSONDecodeError:
        rx_json, report = None, {"safe": False, "issues": ["LLM输出非JSON，需修正Prompt或人工处理"]}
    return {"syndrome": sx, "prescription": rx, "verification": report}

# ── 测试入口 ─────────────────────────────────────────
if __name__ == "__main__":
    result = treatment_pipeline(
        disease="支原体肺炎（模型输出）",
        symptoms="咳嗽频繁，呼吸音粗，痰多，鼻有清涕",
        temp="39.8",
        pig_info="育肥猪，体重约70kg，月龄4个月，中度病情",
    )
    print(json.dumps(result, ensure_ascii=False, indent=2))
```

> 验证点：能输出完整三步结果，辨证与组方中 `references` 均来自检索片段。**若某阶段返回的不是JSON**，加 `response_format={"type": "json_object"}` 或把温度降到0。

### 4.2 诊断模型 → 论治引擎衔接

```python
# bridge.py — 把辨病模型输出转成论治引擎输入
def model_to_syndrome_input(pred_label, prob, extra):
    """把分类结果转换为自然语言描述，喂给辨证阶段"""
    disease_names = {"mycoplasma": "支原体肺炎", "pasteurella": "猪肺疫",
                     "influenza": "猪流感", "healthy": "健康"}
    return f"模型诊断：{disease_names.get(pred_label, pred_label)}，置信度{prob:.2f}，{extra}"
```

### 4.3 关键调优参数

| 参数           | 推荐值        | 说明                              |
| -------------- | ------------- | --------------------------------- |
| 辨证阶段温度   | 0.2-0.3       | 辨证要确定性，低温度              |
| 组方阶段温度   | 0.5           | 组方可略放开，允许合理变化        |
| max_tokens     | 1024-2048     | 组方输出较长                      |
| 检索K值        | 辨证3+典籍5 / 组方3+典籍5 | 命中不足调大，噪声多调小 |
| 重试机制       | 2次，指数退避 | 限流时自动重试                    |

---

## 5. 三大知识库建设（论治层核心工作量）

> 定调：**无需训练**。核心是建好三库，LLM 检索后辨证组方。数据结构化存 CSV/MySQL + 向量化进 ChromaDB（第6节）。

### 5.1 数据库①：症状-疾病库（辨证库，`data/kg/symptom_disease.csv`）

**多维体征 → 标准化证候**，字段对齐探讨预期（体温/咳喘/排泄物等）：

```csv
temperature,cough_type,excretion,other_signs,syndrome,disease,evidence
38.5-40,湿咳+痰多+呼吸粗,正常,食欲下降,疫热壅肺（中期）,猪支原体肺炎,干咳转湿咳、无明显高热
40以上,干咳+鼻流清涕,水样便,精神差、突发,邪袭肺卫（前期）,猪流感,传播快、群发
39-41,咳嗽+呼吸困难,便秘,颈部肿胀,疫热壅肺（中期）,猪肺疫,急性败血症表现、死亡快
38-39.5,咳嗽+喘促,稀溏,精神萎靡、病程长,气阴两伤（后期）,混合感染,反复发作
```

> 字段：`temperature`（体温区间）| `cough_type`（咳喘表现）| `excretion`（排泄物）| `other_signs`（其他体征）| `syndrome`（标准化证候）| `disease` | `evidence`（鉴别依据）

### 5.2 数据库②：疾病-处方库（`data/kg/disease_formula.csv`）

```csv
disease,base_formula,herbs,add_rule,usage,contraindication,reference
疫热壅肺,迎甘组方（PPT已验证）,黄芪30g 甘草25g 黄芩20g 茯苓15g 桔梗5g 满山红5g,痰多加半夏10g;气虚加党参15g,按500g饲料混饲连用5-7天,孕猪减量,PPT三代技术验证
邪袭肺卫,银翘散加减,金银花15g 连翘15g 桔梗10g 薄荷6g 甘草5g,表虚汗多加黄芪20g,按500g饲料混饲,忌用于脾虚便溏,《中兽医学》
气阴两伤,生脉散加减,党参20g 麦冬15g 五味子8g 黄芪20g,余热未清加黄芩10g,按500g饲料混饲,外感未解者忌,《中兽医学》
痰热壅肺,清肺止咳散加减,黄芩15g 桔梗10g 杏仁10g 半夏10g 甘草8g,痰黄黏加石膏20g,按500g饲料混饲,孕猪慎用半夏,《中兽医学》
```

> 字段：`disease`（证型）| `base_formula` | `herbs`（药材+基础剂量）| `add_rule`（加减规则）| `usage` | `contraindication` | `reference`

### 5.3 药材属性表（`data/kg/herbs.csv`，剂量校验用）

```csv
name,property,meridian,effect,dosage_range
黄芪,微温,肺脾,补气升阳、固表止汗,15-30
甘草,平,心肺脾胃,补脾益气、清热解毒、调和诸药,5-25
黄芩,寒,肺胆,清热燥湿、泻火解毒,10-30
茯苓,平,心脾肾,利水渗湿、健脾宁心,10-20
桔梗,平,肺,宣肺利咽、祛痰排脓,5-10
满山红,寒,肺,止咳祛痰,5-15
麻黄,温,肺膀胱,发汗解表、宣肺平喘,5-15
石膏,大寒,肺胃,清热泻火、除烦止渴,15-60
金银花,寒,肺心胃,清热解毒,10-30
连翘,寒,肺心胆,清热解毒、消肿散结,10-30
杏仁,温,肺大肠,降气止咳平喘,5-15
半夏,温,脾胃肺,燥湿化痰、降逆止呕,5-15
陈皮,温,脾胃肺,理气健脾、燥湿化痰,5-15
党参,平,脾肺,补中益气,10-30
麦冬,寒,心肺胃,养阴生津、润肺清心,10-20
五味子,温,肺心肾,收敛固涩、益气生津,3-10
```

### 5.4 数据库③：中医典籍语料（`data/rag/classics/`）

文本切片来源（`.txt`/`.md` 均可，第6节自动切分入库）：

```
data/rag/classics/
├── 中兽医学_辨证论治.txt      # 三期辨证、脏腑辨证章节摘录
├── 猪呼吸道疾病诊疗规范.txt    # 症状描述、用药指导
├── 已验证组方案例.txt          # PPT实操方剂与疗效记录
└── 中药性味归经.txt            # 药材属性（与herbs.csv互补）
```

### 5.5 剂量计算 + 安全校验（规则引擎，非LLM）

```python
# safety.py — 剂量计算与安全规则
WEIGHT_COEF = {  # 体重系数
    (0, 25): 0.7, (25, 60): 1.0, (60, 100): 1.3, (100, 999): 1.5,
}
SEVERITY_COEF = {"轻度": 0.8, "中度": 1.0, "重度": 1.2}

def calc_dosage(base_g, weight, severity):
    """基础剂量 × 体重系数 × 病情系数"""
    wc = next(c for (lo, hi), c in WEIGHT_COEF.items() if lo <= weight < hi)
    sc = SEVERITY_COEF[severity]
    return round(base_g * wc * sc, 1)

# 十八反/十九畏禁忌（简化表，完整表见《中兽医学》）
CONTRAINDICATIONS = [
    ("甘草", "甘遂"), ("甘草", "大戟"), ("甘草", "芫花"), ("甘草", "海藻"),
    ("乌头", "半夏"), ("乌头", "瓜蒌"), ("乌头", "贝母"), ("乌头", "白蔹"), ("乌头", "白及"),
    ("藜芦", "人参"), ("藜芦", "沙参"), ("藜芦", "丹参"), ("藜芦", "玄参"), ("藜芦", "细辛"),
]

def check_contraindications(herbs):
    """输入药材名列表，返回违规组合"""
    names = {h for h in herbs}
    violations = [pair for pair in CONTRAINDICATIONS if set(pair) <= names]
    return violations

def safety_check(prescription):
    """完整安全校验：剂量范围 + 配伍禁忌"""
    issues = []
    # 1. 剂量范围校验（对照 herbs.csv 的 dosage_range）
    # 2. 配伍禁忌
    herbs = [h["name"] for h in prescription["herbs"]]
    issues += check_contraindications(herbs)
    # 3. 特殊状态（孕期禁用等）——按需扩展
    return {"safe": len(issues) == 0, "issues": issues}
```

> 可选增强（时间充裕再做）：用 Neo4j 将疾病-处方关系可视化（ECharts 展示方剂-药材关联），仅演示用，不参与核心推理。

### 5.5 剂量计算 + 安全校验（规则引擎）

```python
# safety.py — 剂量计算与安全规则
WEIGHT_COEF = {  # 体重系数
    (0, 25): 0.7, (25, 60): 1.0, (60, 100): 1.3, (100, 999): 1.5,
}
SEVERITY_COEF = {"轻度": 0.8, "中度": 1.0, "重度": 1.2}

def calc_dosage(base_g, weight, severity):
    """基础剂量 × 体重系数 × 病情系数"""
    wc = next(c for (lo, hi), c in WEIGHT_COEF.items() if lo <= weight < hi)
    sc = SEVERITY_COEF[severity]
    return round(base_g * wc * sc, 1)

# 十八反/十九畏禁忌（简化表，完整表见《中兽医学》）
CONTRAINDICATIONS = [
    ("甘草", "甘遂"), ("甘草", "大戟"), ("甘草", "芫花"), ("甘草", "海藻"),
    ("乌头", "半夏"), ("乌头", "瓜蒌"), ("乌头", "贝母"), ("乌头", "白蔹"), ("乌头", "白及"),
    ("藜芦", "人参"), ("藜芦", "沙参"), ("藜芦", "丹参"), ("藜芦", "玄参"), ("藜芦", "细辛"),
]

def check_contraindications(herbs):
    """输入药材名列表，返回违规组合"""
    names = {h for h in herbs}
    violations = [pair for pair in CONTRAINDICATIONS if set(pair) <= names]
    return violations

def safety_check(prescription):
    """完整安全校验：剂量范围 + 配伍禁忌"""
    issues = []
    # 1. 剂量范围校验（对照 herbs.csv 的安全区间）
    # 2. 配伍禁忌
    herbs = [h["name"] for h in prescription["herbs"]]
    issues += check_contraindications(herbs)
    # 3. 特殊状态（孕期禁用等）——按需扩展
    return {"safe": len(issues) == 0, "issues": issues}
```

---

## 6. RAG 知识库实操

> **⚠️ 推荐方案**：本项目已实现 `rag_retrieve.py`（jieba 哈希词频向量 + 内存余弦检索，零外部下载）。
> 数据量约 250 片段时毫秒级返回，接口与 ChromaDB 兼容。**ChromaDB 方案仅作参考**（写入可用，
> 但其内置 embedding 需从 HuggingFace 下载 79MB——网络受限时极慢；且 hnswlib 查询在 Python 3.14 会卡死）。

### 6.0 直接使用（已就绪，无需构建）

```python
from rag_retrieve import retrieve, stats

print(stats())   # {'symptom_disease': 9, 'disease_formula': 14, 'tcm_classics': 230}
retrieve('symptom_disease', '体温39.2 湿咳 痰多 呼吸粗', k=3)
# → ['【来源：辨证库】体温38.5~40.0℃，咳喘：湿咳、痰多... → 证候：疫热壅肺，疾病：猪支原体肺炎...', ...]
```

> 数据源：`data/kg/symptom_disease.csv` + `data/kg/disease_formula.csv`（从 MySQL 导出）+ `data/rag/classics/*.txt`。每次运行自动构建索引（<1秒）。
> ⚠️ 模式说明：默认 `FULL_KB=True` 时，辨证/论治小库走 `get_all()` 全量注入（不经过检索），`retrieve()` 仅用于典籍库；`FULL_KB=False` 时三库全部走检索（本节的 retrieve 用法即为该模式）。

### 6.1 三库数据源准备

```
data/
├── kg/
│   ├── symptom_disease.csv    # 数据库① 症状-疾病（5.1节）
│   ├── disease_formula.csv    # 数据库② 疾病-处方（5.2节）
│   └── herbs.csv              # 药材属性（5.3节，剂量校验用，可不入库）
└── rag/
    └── classics/              # 数据库③ 中医典籍语料（.txt/.md，5.4节）
        ├── 中兽医学_辨证论治.txt
        ├── 猪呼吸道疾病诊疗规范.txt
        ├── 已验证组方案例.txt
        └── 中药性味归经.txt
```

### 6.2 三库向量化构建（ChromaDB，本地零配置）

```python
# build_rag.py — 构建三个collection：symptom_disease / disease_formula / tcm_classics
import chromadb
from langchain.text_splitter import RecursiveCharacterTextSplitter
import csv, os

client = chromadb.PersistentClient(path="data/chroma_db")
splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)

def _add_docs(collection, docs, sources, doc_id):
    for d, s in zip(docs, sources):
        collection.add(ids=[f"doc{doc_id}"], documents=[d], metadatas=[{"source": s}])
        doc_id += 1
    return doc_id

def build_all():
    doc_id = 0
    # ── ① 辨证库：CSV每行（多维体征）转自然语言条目 ──
    col_sym = client.get_or_create_collection("symptom_disease", metadata={"hnsw:space": "cosine"})
    sym_docs, sym_src = [], []
    with open("data/kg/symptom_disease.csv", encoding="utf-8") as f:
        for row in csv.DictReader(f):
            sym_docs.append(
                f"体温{row['temperature']}℃，咳喘：{row['cough_type']}，"
                f"排泄物：{row['excretion']}，其他体征：{row['other_signs']} → "
                f"证候：{row['syndrome']}，疾病：{row['disease']}，鉴别依据：{row['evidence']}"
            )
            sym_src.append("辨证库")
    doc_id = _add_docs(col_sym, sym_docs, sym_src, doc_id)
    print(f"症状-疾病库: {len(sym_docs)} 条")

    # ── ② 疾病-处方库：CSV每行转自然语言条目 ──
    col_rx = client.get_or_create_collection("disease_formula", metadata={"hnsw:space": "cosine"})
    rx_docs, rx_src = [], []
    with open("data/kg/disease_formula.csv", encoding="utf-8") as f:
        for row in csv.DictReader(f):
            rx_docs.append(
                f"证型/疾病：{row['disease']} → 基础方剂：{row['base_formula']}，"
                f"组成：{row['herbs']}，加减：{row['add_rule']}，"
                f"用法：{row['usage']}，禁忌：{row['contraindication']}，出处：{row['reference']}"
            )
            rx_src.append("疾病-处方库")
    doc_id = _add_docs(col_rx, rx_docs, rx_src, doc_id)
    print(f"疾病-处方库: {len(rx_docs)} 条")

    # ── ③ 中医典籍库：文本切块 ──
    col_med = client.get_or_create_collection("tcm_classics", metadata={"hnsw:space": "cosine"})
    med_docs, med_src = [], []
    for root, _, files in os.walk("data/rag/classics"):
        for f in files:
            if not f.endswith((".txt", ".md")):
                continue
            path = os.path.join(root, f)
            with open(path, encoding="utf-8") as fh:
                text = fh.read()
            for chunk in splitter.split_text(text):
                med_docs.append(chunk)
                med_src.append(path)
    doc_id = _add_docs(col_med, med_docs, med_src, doc_id)
    print(f"中医典籍库: {len(med_docs)} 条")

if __name__ == "__main__":
    build_all()
```

> 注意：ChromaDB 默认内置 embedding（onnx 模型，自动下载）。也可换用百炼 `text-embedding-v3`。重建库时先 `client.delete_collection(name)` 再建。

### 6.3 检索质量检查（先验证库，再跑引擎）

```python
# check_rag.py — 验证三库检索相关性
import chromadb
client = chromadb.PersistentClient(path="data/chroma_db")

def check(collection_name, query, k=3):
    col = client.get_collection(collection_name)
    res = col.query(query_texts=[query], n_results=k)
    print(f"\n=== {collection_name} 检索: {query} ===")
    for d, m in zip(res["documents"][0], res["metadatas"][0]):
        print(f"  [{m.get('source','?')}] {d[:80]}...")

if __name__ == "__main__":
    check("symptom_disease", "湿咳 痰多 呼吸粗")
    check("disease_formula", "疫热壅肺 组方")
    check("tcm_classics", "支原体肺炎 辨证 治则")
```

> 验证点：每个库 Top-3 返回与查询相关的条目。检索不相关时：①查数据本身质量 ②换 embedding ③用 MMR 去重。

---

## 7. 集成联调与验收

### 7.1 端到端测试脚本

```python
# e2e_test.py — 模拟完整链路：辨病模型 → RAG论治引擎 → 组方 → 校验
import numpy as np
from train_svm_baseline import model  # 或加载已训练模型
from therapy_engine import treatment_pipeline
from bridge import model_to_syndrome_input

TEST_CASES = [
    {
        "audio": "data/processed/test/mycoplasma/D001.wav",
        "expected": "支原体肺炎",
        "temp": 39.8, "mental": "精神差", "env": "湿度75%",
        "symptoms": "湿咳、痰多、呼吸粗",
        "pig": "育肥猪70kg/4月龄，中度病情",
    },
    # 继续添加测试用例...
]

def run_e2e():
    for i, case in enumerate(TEST_CASES):
        print(f"=== 用例{i+1}: {case['expected']} ===")
        # 1) 特征提取 + 辨病模型预测
        vec = extract_features(case["audio"])
        pred_label = model.predict([vec])[0]
        prob = model.predict_proba([vec])[0].max()
        disease = model_to_syndrome_input(pred_label, prob, "湿咳、痰多")
        # 2) 论治引擎（RAG检索 + LLM辨证组方 + 规则校验）
        result = treatment_pipeline(
            disease=disease,
            symptoms=case["symptoms"],
            temp=case["temp"],
            pig_info=case["pig"],
        )
        # 3) 输出结果
        print(f"模型预测: {pred_label} ({prob:.2f})")
        print(f"辨证: {result['syndrome']}")
        print(f"组方: {result['prescription']}")
        print(f"校验: {result['verification']}")
        print("---")

if __name__ == "__main__":
    run_e2e()
```

### 7.2 验收标准清单

| #  | 验收项        | 通过标准                                     |
| -- | ------------- | -------------------------------------------- |
| 1  | 百炼 API 调用 | 有调用凭证/截图，qwen 返回正常               |
| 2  | 诊断模型      | 测试集准确率 ≥85%，有混淆矩阵图             |
| 3  | 三库建设      | 症状-疾病/疾病-处方 ≥20条、典籍语料 ≥4篇    |
| 4  | RAG 检索      | `check_rag.py` 三库 Top-3 均相关             |
| 5  | 辨证阶段      | 输出严格JSON，辨证合理（专家抽查5例通过）    |
| 6  | 组方阶段      | 处方含药材/剂量/用法/禁忌，引用真实          |
| 7  | 规则校验      | 100%检测出故意植入的配伍禁忌                 |
| 7  | RAG           | 检索Top-3相关度可人工确认                    |
| 8  | 端到端        | 5个测试用例全流程跑通，总耗时<60秒           |
| 9  | 前端页面      | 柴堰颖侧：诊断结果展示页可调用本板块API      |
| 10 | 技术文档      | 含架构图、实验设计、Baselines、Metrics、结果 |

### 7.3 交付给整合板块的 API 约定

**`POST /api/diagnosis`**

```json
// 请求
{
  "audio_feature": [0.12, 0.45, ...],   // 142维特征向量
  "temp_c": 39.8,
  "mental_state": "精神差",
  "env": {"humidity": 75, "ammonia": "medium"},
  "pig": {"weight_kg": 70, "age_month": 4}
}

// 响应
{
  "diagnosis": {"label": "mycoplasma", "disease": "支原体肺炎", "confidence": 0.87},
  "syndrome": {"stage": "中期", "syndrome": "疫热壅肺", "principle": "清热化痰，宣肺平喘"},
  "prescription": {"base_formula": "麻杏石甘汤加减", "herbs": [...], "preparation": "按500g饲料混饲"},
  "verification": {"approved": true, "safety_score": 0.92},
  "trace_id": "20260804-001"
}
```

---

## 8. 常见问题排查

| 现象                              | 原因                | 解决                                                                         |
| --------------------------------- | ------------------- | ---------------------------------------------------------------------------- |
| `pip install torch` 下载慢      | 网络                | 用清华源`-i https://pypi.tuna.tsinghua.edu.cn/simple`                      |
| 百炼返回`InvalidApiKey`         | 密钥错误/未开通     | 检查`.env`、重新复制密钥、确认百炼已开通                                   |
| 百炼返回`Throttling` 限流       | 调用过频            | 加`time.sleep(1)`、换 qwen-turbo、重试机制                                 |
| Qwen 输出不是JSON                 | 温度太高/提示词不严 | `temperature=0.2`，加 `response_format={"type":"json_object"}`           |
| `librosa.output.write_wav` 报错 | 新版librosa移除     | 改用`import soundfile as sf; sf.write(path, y, sr)`                        |
| ChromaDB 打开库失败              | 库被重建/路径错     | 确认 `data/chroma_db` 路径一致，重建前先 `client.delete_collection()`         |
| ChromaDB 下载 embedding 模型失败  | 网络                | 用国内源或预下载 onnx 模型，或换 Qwen embedding                              |
| 训练时显存不足                    | batch 太大          | batch_size 减半 32→16，或 seq 长度缩短                                      |
| 分类准确率<70%                    | 数据问题为主        | 检查：样本数量、标注一致性、类别均衡、特征是否正常                           |
| 标签不平衡（某类极少）            | 现实常态            | 用类别权重`class_weight='balanced'` 或 ROS 上采样，或仅先做二分类          |
| 兽医说辨证不对                    | 中医知识不足        | 把专家批注加入RAG语料 + 收集反馈进SFT数据                                    |
| 幻觉引用（组方编造出处）          | LLM通病             | **必加**RAG检索注入 + 规则校验引擎查引用 + prompt强调"无依据禁止给" |

---

## 9. 进度自查表

| 阶段                         | 截止   | 状态 | 负责人 |
| ---------------------------- | ------ | ---- | ------ |
| □ 环境搭建 + 百炼API凭证    | Day 1  |      | 阴学舟 |
| □ 诊断数据库设计 + 标注表   | Day 2  |      | 刘宇翔 |
| □ 音频预处理 + 特征提取跑通 | Day 3  |      | 刘宇翔 |
| □ SVM baseline 训练         | Day 4  |      | 刘宇翔 |
| □ CNN-BiLSTM / 集成优化     | Day 6  |      | 阴学舟 |
| □ 三库数据整理（症状-疾病/疾病-处方/典籍） | Day 5 |      | 柴堰颖 |
| □ RAG检索 + LLM两阶段流水线跑通 | Day 8  |      | 阴学舟 |
| □ RAG 知识库 + 检索注入     | Day 10 |      | 阴学舟 |
| □ 组方API + 安全校验        | Day 11 |      | 刘宇翔 |
| □ 前端诊断页对接            | Day 13 |      | 柴堰颖 |
| □ 端到端联调 + 5用例测试    | Day 14 |      | 全员   |
| □ 技术文档 + 演示材料       | Day 15 |      | 全员   |

> 每3天工作慰问节点前，对照此表更新状态。
