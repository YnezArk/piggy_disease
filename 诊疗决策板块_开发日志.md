# 诊疗决策板块 — 开发日志

> 板块：诊疗决策（辨病 + 辨证组方）| 负责人：阴学舟、刘宇翔、柴堰颖
> 周期：2026-08-04 ~ 2026-08-09
> 基座模型：Qwen（百炼自定义网关，现用 `qwen3.7-plus`）| 数据库：MySQL `pig_diag`

---

## 一、开发时间线

### 2026-08-04 — 需求理解与技术路线

- 阅读全部文献资料（项目方案 docx、三代技术 PPT、AI Scientist 赛题、5 篇猪咳嗽识别论文）
- 解析《猪咳嗽项目安排.xlsx》：确认板块分工（诊疗决策=阴学舟/刘宇翔/柴堰颖）、8/5 技术路线截止、硬性要求（Qwen 基座 + 百炼平台）
- 产出《诊疗决策板块_技术路线报告.md》（初版）

### 2026-08-05 — 架构定调与实操手册

- 负责人明确**两层认知模型**定调：辨病=轻量多分类模型（数据要求低）、论治=LLM+RAG（无需训练）
- 技术路线报告重构：四Agent流水线 → **三库RAG + LLM两阶段生成 + 规则校验**，SFT 降级为可选
- 对齐《探讨预期.docx》：意图路由、Text-to-SQL、辨证库多维体征、Agent 术语、知识图谱口径
- 产出《诊疗决策板块_实操手册.md》（环境/数据/模型/RAG/联调全流程 + 15 天计划）

### 2026-08-06 — 数据库设计与落地

- 产出《诊疗决策板块_数据库设计.md》：12 张表（知识库 9 + 业务库 3），理-法-方-药逻辑链落库
- `db_schema.sql` 建库成功（MySQL 8.4.7 @ localhost:3306），种子数据 + 查询示例验证通过
- 修正种子数据错误：十八反药材缺失（子查询返回 NULL）、麻黄误当乌头、"五味子畏党参"错误知识

### 2026-08-07 — 数据爬取与知识库扩充

- 4 路并行 agent 爬取：猪呼吸道疾病知识（5病63来源）、三期辨证（44来源）、经典方剂（13首含兽用剂量）、25 味药材（药典剂量+禁忌）
- 关键诚实发现：**"邪袭肺卫→疫热壅肺→气阴两伤"三期命名为文献整合产物**（《畜禽温病》卫气营血分期+人医流感方案），如实标注防幻觉审查
- `db_seed_extend.sql` 扩充入库：herb 21→48、formula 4→14、disease 4→5、syndrome 5→7、辨证规则 4→9
- RAG 检索落地：ChromaDB 写入成功但 **hnswlib 在 Python 3.14 查询卡死** → 自研 `rag_retrieve.py`（jieba 哈希向量 + 内存余弦，毫秒级）
- 修复导出笛卡尔积 bug（LEFT JOIN add_rule 导致组方重复）

### 2026-08-08 — 论治引擎与测试体系

- `therapy_engine.py` 落地：查询改写 → RAG 检索 → 辨证Agent → 组方Agent → 校验Agent 全链路
- 关键决策：**校验不依赖 LLM**——剂量/十八反/幻觉拦截由规则引擎保证
- `api_server.py`（FastAPI）+ `interactive_demo.py`（交互终端）+ `test_manual.py`（6 块测试套件）
- 修复剂量参照系矛盾：herb 表人用区间 vs 兽用整方配比（5-6倍差）→ 分级校验（软提示/8倍硬拦截）
- 5 案例 17 项测试全绿

### 2026-08-09 — 架构升级与全面审查

- **查询改写层**（LLM 生成检索指令）：口语输入规范化，双路检索（向量+SQL），对齐探讨预期"第一步检索"
- **辨病纠错机制**（双重交叉验证）：规则验证（非LLM）+ LLM 独立判断（disease_conflict），实测错判场景成功纠正
- **知识注入分层**（FULL_KB=True）：小库全量注入（辨证9条/论治14条 → LLM 语义匹配），典籍库保持检索
- **辨证稳定性修复**：温度 0.2→0.0、特异体征优先原则（犬坐/张口呼吸→疫热壅肺）、SQL 多关键词 OR + evidence 字段 + 标点分词、同义词归一（犬类坐→犬坐/高温→高热）
- **全面代码审查**：pyflakes + 逐文件细读，修复 13 处问题（未定义变量 `kw`、call_qwen 重复定义、calc_dosage 越界崩溃、死代码、过时注释、未用导入等）
- 环境配置集中化：`.env`（AI 模型 + 数据库）+ `config.py` 统一读取
- 模型升级：`qwen3.7-flash` → `qwen3.7-plus`（用户调整）
- 测试结果：38/38 全绿（env 3 + rag 6 + safety 5 + sql 4 + cases 17 + 边界 3）

---

## 二、完成内容清单

### 📄 文档（6 份）

| 文档 | 状态 | 说明 |
|------|------|------|
| 技术路线报告.md | ✅ | 两层认知模型架构、知识注入分层策略、实验设计、Baselines/Metrics |
| 实操手册.md | ✅ | 环境搭建→数据→模型→RAG→联调→验收，全代码可照抄 |
| 数据库设计.md | ✅ | 12 表设计、ER 关系、数据字典、检索对接 |
| 运行流程说明.md | ✅ | 系统全景、五步链路、代码对照、部署、故障排查 |
| 测试手册.md | ✅ | 测试样例、预期结果表、边界场景、验收对照 |
| 开发日志.md | ✅ | 本文 |

### 💻 代码（8 个可运行脚本）

| 文件 | 功能 | 验证 |
|------|------|------|
| `config.py` | 环境配置统一读取（.env） | ✅ |
| `test_qwen.py` | 网关连通性测试 | ✅ |
| `rag_retrieve.py` | 三库内存检索（jieba哈希+余弦） | ✅ 3 查询命中 |
| `safety.py` | 校验Agent：剂量换算/十八反/幻觉拦截/孕猪 | ✅ 5 项 |
| `therapy_engine.py` | 论治引擎：改写→检索→辨证→组方→校验 | ✅ 5 案例 |
| `api_server.py` | FastAPI：POST /api/diagnosis + 落库 | ✅ RX 落库 |
| `interactive_demo.py` | 交互式手动诊断终端 | ✅ |
| `test_manual.py` | 手动测试套件（6 块 24 项） | ✅ 38/38 |

### 🗄️ 数据资产

| 资产 | 规模 |
|------|------|
| MySQL `pig_diag` | 13 来源 / 5 疾病 / 7 证候 / 9 辨证规则 / 48 药材 / 14 方剂 / 73 组成 / 11 加减 / 8 禁忌 |
| 典籍语料 | 2 篇 212+111 行（三期辨证、诊疗规范） |
| 网络爬取原始资料 | 方剂 13 首、药材 25 味（含来源 URL） |
| RAG 索引 | 辨证 9 + 论治 14 + 典籍 230 = 253 片段 |

---

## 三、关键决策记录（踩坑与取舍）

| # | 决策 | 原因 |
|---|------|------|
| 1 | **ChromaDB 弃用 → 内存检索** | 内置 embedding 需下载 79MB（网络被墙）；hnswlib 查询在 Python 3.14 卡死。253 片段内存余弦毫秒级足够 |
| 2 | **校验不依赖 LLM** | 安全必须确定性：十八反/剂量硬拦截用规则引擎，LLM 只做辨证组方 |
| 3 | **剂量参照系分离** | herb 表人用区间 vs 兽用整方配比差 5-6 倍 → 软提示 + 8 倍幻觉硬拦截 |
| 4 | **小库全量注入（FULL_KB）** | 9+14 条仅 3.5k token；LLM 语义匹配（"老咳"≈"连咳"）远强于弱向量；典籍 230 段保持检索 |
| 5 | **辨病纠错双重验证** | 上游模型可能出错：规则验证（非LLM）+ LLM 独立判断，矛盾时按症状辨证并警告 |
| 6 | **辨证温度 0.0** | 实测同体征两次结果漂移（42℃犬坐被判流感/胸膜肺炎不一致）→ 确定性场景零随机 |
| 7 | **特异体征优先原则** | 犬坐/张口呼吸/血性泡沫 = 肺气壅闭重症标志，权重高于"突发高热"泛化体征 |
| 8 | **查询改写 + 规则词双保险** | LLM 改写不稳定时，同义词归一表（犬类坐→犬坐）保证 SQL 检索不失效 |
| 9 | **三期辨证命名如实标注** | "邪袭肺卫→疫热壅肺→气阴两伤"为文献整合产物，防幻觉审查（赛题硬性要求引用真实） |

---

## 四、测试验证总览

```
环境检查  3/3  ✅
RAG检索   6/6  ✅（三库六查询全命中预期证候）
安全校验  5/5  ✅（十八反/幻觉500g/孕猪/剂量换算）
SQL查询   4/4  ✅（辨证查询/组方查询/禁忌/引用完整性）
端到端    17/17 ✅（案例A-E：辨证/组方/校验/纠错全部符合预期）
边界      3/3  ✅（体重越界兜底/标点分词/用户场景复现稳定）
────────────────────────────
合计      38/38 ✅
```

**端到端案例覆盖**：支原体中期（疫热壅肺/清肺散）、流感前期（邪袭肺卫/银翘散）、肺疫重症（疫热壅肺/麻杏石甘汤）、混合感染后期（气阴两伤/生脉散）、辨病纠错（错判支原体→按症状定流感并双重警告）。

---

## 五、当前状态与待办

### ✅ 已完成
- 论治链路全通（查询改写→双路检索→辨证→组方→校验→落库→展示）
- 辨病纠错、安全护栏、引用溯源、交互演示、自动测试体系
- 环境配置集中化（.env + config.py）

### ⏳ 待办（按优先级）
1. **辨病模型训练**：等监测板块音频数据 → SVM baseline（手册第 3 节代码就绪）
2. **前端对接**（柴堰颖）：小程序调 `/api/diagnosis`，展示证型/组方/校验/引用
3. **疗效反馈接口**：`POST /api/feedback`（treatment_feedback 表已建）
4. **知识库扩充**：辨证规则/方剂数据按需增补（当前 9 条/14 首为最小集）
5. **规则核验优化**：SQL 命中证据字段覆盖（已做），可继续加"全部命中展示"（已做）

### ⚠️ 已知局限（可接受）
- `_extract_json` 贪婪匹配（LLM 输出多 JSON 块时异常——罕见有兜底）
- `check_contraindications` 每次连 MySQL（本地可接受）
- 边界案例（信息不足时证候判定有主观性）留待兽医专家裁决

---

## 六、运行方式速查

```bash
# 测试
python test_manual.py all           # 全量自测（api 块需先起服务）
python test_manual.py cases         # 端到端 5 案例

# 交互演示
python interactive_demo.py          # 手动输入诊断

# API 服务（前端对接）
python -m uvicorn api_server:app --host 0.0.0.0 --port 8000
# 文档: http://localhost:8000/docs

# 网关测试
python test_qwen.py
```
