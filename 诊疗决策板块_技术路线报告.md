# 诊疗决策板块 — 技术路线报告

> **项目**：音甘猪咳嗽声智能监测与诊疗辅助决策系统  
> **板块**：板块4 — 诊疗决策（诊断-辨证-治疗-评估闭环）  
> **拟负责人**：阴学舟（主责）、刘宇翔、柴堰颖  
> **日期**：2026-08-04  
> **基座模型**：千问（Qwen-Max/Plus/Turbo）| 平台：阿里云百炼

---

## 一、板块定位与目标

### 1.1 在系统中的位置

诊疗决策板块位于"监测→**诊断→治疗**→评估"闭环的**核心决策层**，承接监测板块（杨宇航/郭悦负责）输出的咳嗽检测结果，完成从"检测到咳嗽"到"诊断出疾病、给出治疗方案"的智能跃迁。

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  监测板块  │───▶│ 诊疗决策板 │───▶│  整合板块  │───▶│  前端展示  │
│ (咳嗽识别) │    │ (本板块)  │    │ (周海斌)  │    │ (小程序)  │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
      诊断模型       辨证组方引擎
     (辨病·多分类)   (论治·LLM+RAG)
```

### 1.2 核心目标

| 维度 | 目标 |
|------|------|
| **诊断** | 基于咳嗽声学特征+多模态数据（体温、精神状态），实现疾病类型多分类，准确率 ≥ 90% |
| **辨证** | 融合中医辨证论治体系（三期分型：邪袭肺卫→疫热壅肺→气阴两伤），实现可解释的病程推理 |
| **治疗** | 基于 RAG 检索（疾病-处方库）+ LLM 推理，自动生成中药组方建议，包含药材、剂量、用法 |
| **评估** | 建立治疗反馈机制，支持组方调整与疗效追踪 |

---

## 二、技术架构总览（两层认知模型）

```
┌─────────────────────────────────────────────────────────────┐
│                  诊疗决策板块架构                              │
│            两层认知模型 + RAG 知识底座                         │
├─────────────────────────────────────────────────────────────┤
│  监测模块输入：咳嗽声学特征 ─ 体温 ─ 精神状态 ─ 环境参数          │
│                        │                                     │
│                        ▼                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  认知层·辨病：疾病诊断模型（轻量多分类）                  │   │
│  │                                                       │   │
│  │  输入：特征向量（MFCC/logFBank/时频统计）                │   │
│  │  模型：SVM / CNN-BiLSTM 多分类（数据量要求低）           │   │
│  │  输出：疾病诊断（如支原体肺炎）+ 置信度                   │   │
│  └───────────────────────┬──────────────────────────────┘   │
│                          ▼                                   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  认知层·论治：辨证组方模型（LLM + RAG，无需训练）         │   │
│  │                                                       │   │
│  │   ┌───────────┐  ┌───────────┐  ┌────────────────┐  │   │
│  │   │ 症状-疾病  │  │ 疾病-处方  │  │ 中医典籍语料库   │  │   │
│  │   │ 数据库     │  │ 数据库     │  │ (向量化+全文)   │  │   │
│  │   └─────┬─────┘  └─────┬─────┘  └───────┬────────┘  │   │
│  │         │   RAG 检索增强                  │           │   │
│  │         └─────────┬───┴──────────────────┘           │   │
│  │                   ▼                                   │   │
│  │         Qwen LLM：辨证推理 + 个性化组方 + 安全校验       │   │
│  └───────────────────────┬──────────────────────────────┘   │
│                          ▼                                   │
│          输出：证型 / 治则 / 组方 / 用法 / 禁忌 / 引用出处       │
└─────────────────────────────────────────────────────────────┘
```

### 两层决策流水线

```
监测特征 ──▶ [辨病层] 疾病分类 ──▶ [论治层] RAG检索 + LLM辨证组方 ──▶ 诊疗方案
              (轻量多分类)         (症状-疾病 / 疾病-处方 / 中医典籍)
```

---

## 三、核心技术路线

### 3.1 诊断模型（Diagnosis Model）

#### 3.1.1 问题定义

**输入**：咳嗽声学特征向量 + 多模态辅助特征  
**输出**：疾病类别概率分布 P(disease | features)，疾病类别数 K（猪肺疫、支原体肺炎、猪流感、PRRS等）

#### 3.1.2 特征工程

**声学特征（主特征）**：

| 特征类别 | 具体特征 | 维度 | 来源依据 |
|----------|---------|------|---------|
| MFCC | 13维静态 + Δ + Δ² | 39维 | 纪楠(2023)、付小朋(2025) |
| logFBank | 64维 + Δ + Δ² | 192维 | 王梦雨(2022) |
| 语谱图特征 | LBP + HOG + CNN深度特征 | 变长 | 王锡鹏(2024) |
| 时域特征 | 短时能量、过零率、RMS | 3维 | 纪楠(2023) |
| 频域特征 | 频谱质心、带宽、平坦度、滚降度 | 6维 | 纪楠(2023) |

**多模态辅助特征**：

| 模态 | 特征 | 采集方式 |
|------|------|---------|
| 体温 | 红外测温数值、体温变化趋势 | 红外检测仪 |
| 精神状态 | 活动频率、站卧姿态、长期静止时间 | 摄像头 + 行为识别模型 |
| 环境 | 温湿度、氨气浓度、通风状态 | 环境传感器 |

#### 3.1.3 模型架构

**方案A：多域特征融合 + 集成学习（推荐主方案）**

基于纪楠(2023)的 A-TF 架构 + SSLRB 混合集成：

```
                    ┌─────────────┐
    MFCC特征 ──────▶│             │
    logFBank ──────▶│ 特征拼接融合 │──▶ SVM-Stacking ──▶ LR-Bagging ──▶ 疾病类别
    语谱图深度特征 ─▶│             │      (第一层)        (第二层)
    时频域统计特征 ─▶│             │
                    └─────────────┘
```

- **第一层 SVM-Stacking**：多个不同超参数的 SVM 基分类器（RBF核，C=100, γ∈{0.1, 0.01, 0.001}），3折分层交叉验证生成元特征
- **第二层 LR-Bagging**：M=3 个逻辑回归元学习器 Bagging 集成，配合随机上采样（ROS）处理类别不平衡
- **优势**：在重度不平衡数据集F1仍达 92.19%（纪楠2023），适配猪呼吸道疾病天然分布不均的现实

**方案B：CNN-BiLSTM 多分类（备选方案）**

基于付小朋(2025)的二分类模型扩展为多分类：

```
MFCC特征图 ──▶ Conv2D(32,3×3) ──▶ MaxPool ──▶ Conv2D(64,3×3) ──▶ MaxPool
                                                                      │
                                                                      ▼
                                            Softmax(K类) ◀── Dense ◀── BiLSTM(64) ──▶ BiLSTM(32)
```

- 2层CNN提取局部声学特征 → BiLSTM双向建模时序依赖 → 全连接+Softmax输出K类概率
- 可引入 SENet 注意力机制增强关键频段感知

**方案C：多模态对齐融合（进阶方案）**

```
 声学特征 ──▶ 声学编码器(CNN-BiLSTM) ──▶ ┐
 体温数据 ──▶ 数值编码器(MLP) ────────▶ ├── 跨模态注意力融合 ──▶ 分类头
 行为特征 ──▶ 时序编码器(LSTM) ───────▶ ┘
```

#### 3.1.4 训练策略

| 参数 | 设定 |
|------|------|
| 优化器 | Adam，初始学习率 0.001，余弦退火衰减 |
| 批次大小 | 32-64 |
| 训练轮数 | 50-100，Early Stopping (patience=15) |
| 损失函数 | 交叉熵（可加权处理类别不平衡） |
| 验证策略 | 5折分层交叉验证 |
| 数据增强 | 时间拉伸(0.75-1.25x)、音高偏移、加噪(SNR 5-15dB)、时移 |
| 正则化 | Dropout 0.5、L2权重衰减 1e-4 |

#### 3.1.5 评价指标

- **总体**：Accuracy、Macro-F1、Weighted-F1
- **各类别**：Precision、Recall、F1、混淆矩阵
- **不平衡鲁棒性**：PR-AUC、G-mean
- **Baselines**：单一SVM、RF、XGBoost、CNN、LSTM、BiLSTM、Transformer

---

### 3.2 辨证组方引擎（认知层·论治）

#### 3.2.1 设计思路

论治层是**知识密集型任务**，核心依赖 **RAG 架构，无需大量数据从头训练**——直接使用现有大语言模型（Qwen），通过检索三大数据库完成中医辨证与个性化组方：

```
辨病结果 + 临床症状
      │
      ▼
┌───────────────────────────────┐
│  RAG 检索增强：三库并行检索     │
│  ① 症状-疾病库  → 佐证证型     │
│  ② 疾病-处方库  → 基础方剂     │
│  ③ 中医典籍库   → 治则依据     │
└───────────────┬───────────────┘
                ▼
      Qwen LLM：辨证推理 + 组方生成
                ▼
      证型 / 治则 / 组方 / 剂量 / 禁忌（含引用出处）
```

本板块承接 PPT 三代技术已确立的三期辨证框架：

```
前期（邪袭肺卫）──▶ 中期（疫热壅肺）──▶ 后期（气阴两伤）
  表证、卫分证        里证、气分证        虚证、气阴亏虚
```

#### 3.2.2 三大知识数据库（本板块核心工作量）

> 两个核心库（辨证库/论治库）+ 典籍语料，打通 **理-法-方-药** 逻辑链：`[多维症状] ➔ [证候] ➔ [治则] ➔ [基础方] ➔ [加减规则] ➔ [具体药物及剂量]`

**数据库①：症状-疾病库（辨证库）—— 多维体征映射证候**

| 字段 | 说明 | 示例 |
|------|------|------|
| temperature | 体温区间 | 38.5-40℃ |
| cough_type | 咳喘表现 | 湿咳、痰多、呼吸粗 |
| excretion | 排泄物观察 | 粪便干结/稀溏 |
| other_signs | 其他体征（精神/采食等） | 食欲下降、精神萎靡 |
| syndrome | 标准化证候 | 疫热壅肺（中期） |
| disease | 对应疾病 | 猪支原体肺炎 |
| evidence | 鉴别依据 | 干咳转湿咳、无明显高热 |

**数据库②：疾病-处方库**

| 字段 | 说明 | 示例 |
|------|------|------|
| disease | 疾病/证型 | 疫热壅肺 |
| base_formula | 基础方剂 | 麻杏石甘汤加减 |
| herbs | 药材+基础剂量 | 麻黄15g、石膏30g... |
| add_rule | 加减化裁规则 | 痰多加桔梗、半夏；气虚加黄芪 |
| usage | 用法 | 按500g饲料混饲，连用5-7天 |
| contraindication | 禁忌 | 孕猪慎用麻黄 |
| reference | 出处 | 《中兽医学》/临床病例 |

**数据库③：中医典籍语料库**

| 内容 | 说明 |
|------|------|
| 《中兽医学》教材文本 | 辨证论治、脏腑辨证章节切片 |
| 猪呼吸道疾病诊疗规范 | 症状描述、用药指导 |
| 已验证组方案例 | PPT三代技术中的实操方剂（黄芪30g、甘草25g、黄芩20g、茯苓15g、桔梗5g、满山红5g 等） |
| 中药性味归经表 | 300+味药材属性（向量化） |

**数据来源**：中兽医学教材/专著摘录 + 兽医专家标注（殷中琼等团队）+ PPT已验证方剂 + 文献提取。

#### 3.2.3 知识注入流程（分层策略）

```
输入（疾病 + 症状 + 体征）
  │
  ├─▶ 意图解析与路由：LLM判断当前需求 → 决定注入哪个库
  │       （辨病输入 → 辨证库；辨证结果 → 论治库）
  │
  ├─▶ 小库全量注入（FULL_KB=True，默认）——语义匹配交给 LLM
  │      ① 辨证库（9条规则）全量 → LLM 逐一匹配、全局权衡（主症优先）
  │      ② 论治库（14首方剂）全量 → LLM 自选最匹配方剂
  │
  ├─▶ 大库检索（数据大，全量不划算）
  │      ③ 中医典籍库（230段）→ 向量检索 Top5（查询词来自查询改写层）
  │
  └─▶ Text-to-SQL 精确检索（MySQL 辨证库）
        体温区间 + 咳喘关键词 → 精确命中证候规则（证据增强）
  │
  ▼
注入 LLM Prompt（全部带出处标签）
  │
  ▼
Qwen 辨证组方输出（严格JSON）
```

要点：
- **分层理由**：小库（辨证库9条/论治库14条）全量仅约 3.5k token，全量注入让 LLM 做语义模式匹配（"老咳个不停"≈"连咳"），远强于弱向量检索的字面匹配，且消除漏检风险；典籍库 230 段全量约 15k+ token 且冗余多，保持检索
- **主症优先原则**（提示词硬约束）：咳嗽类型/体温/呼吸为主症，精神/食欲/排泄物为兼症；不得因单一兼症推翻主症指向的证候
- **查询改写层**（Step 0）：LLM 将口语化输入规范化为检索词（供典籍库检索与 SQL 精确检索），失败自动回退原始输入
- **Text-to-SQL 精确检索**：LLM 给出体温区间判断，按字段精确匹配（`体温 BETWEEN 38.5 AND 40 AND 咳喘 LIKE '%湿咳%'`），与向量检索互补
- **回退开关**：数据量增长（如辨证规则上百条）时设 `FULL_KB=False` 回退全检索模式
- **命中片段必须带出处**：`【来源：...】` 格式，LLM 输出引用时沿用
- **无命中降级**：检索结果为空时，LLM 仅基于自身知识输出并在结果中标记"低置信度-缺检索依据"，绝不编造出处

#### 3.2.4 LLM 辨证组方生成（Prompt 设计）

采用**角色化 Agent 两阶段生成**（辨证Agent → 组方Agent），即同一 LLM 以不同角色 Prompt 分阶段调用，比多 Agent 服务更轻量可靠；每阶段独立调用、独立校验。辨证温度 0.0（确定性）、组方 0.5（允许合理变化）。

**阶段一：辨证Agent 推理 Prompt**（含辨病纠错与主症原则）

```
系统角色：资深中兽医专家，精通风寒/风热/湿热/气虚等辨证论治。
你有以下检索到的权威资料（不可虚构，无依据必须说明）：
[辨证库全部规则] ...   [中医典籍检索结果] ...
[SQL精确命中候选] ...  [规则交叉验证结果] ...

⚠️ 硬约束：
1. 辨病结果仅作参考（同病异证：证候由体征决定，处方跟证候走）
2. 症状与辨病矛盾时按症状判定，并在 disease_conflict 中明确指出
3. 主症优先：咳嗽/体温/呼吸定证；精神/食欲/排泄物佐证
4. 特异体征优先：犬坐/张口呼吸/血性泡沫 = 肺气壅闭重症标志（疫热壅肺），
   权重高于"突发高热"等泛化体征，同时出现时按重症定证

任务：根据输入完成辨证，输出严格JSON：
{
  "stage": "前期/中期/后期",
  "syndrome": "证型",
  "principle": "治则治法",
  "evidence": ["辨证依据"],
  "references": ["引用出处（来自检索资料）"],
  "disease_conflict": {"upstream_disease": "上游辨病",
                       "evidence_disease": "证据指向的疾病",
                       "conflict": true/false,
                       "reason": "矛盾说明"}
}
```

**辨病纠错（双重交叉验证）**：
- ① 确定性规则验证（非LLM）：SQL 多关键词（含同义词归一：犬类坐→犬坐/高温→高热）查辨证库全部命中规则 → 规则映射疾病 vs 上游病名 → 任一矛盾即告警
- ② LLM 独立判断：`disease_conflict` 按症状独立判定证据指向疾病，与上游对比
- 实测：错判"支原体"但呈"42℃+犬坐"→ 双重指出矛盾，辨证疫热壅肺（胸膜肺炎方向），组方麻杏石甘汤 ✅

**阶段二：组方Agent 生成 Prompt**

```
系统角色：中兽药师，方剂学专家。
你有以下检索到的权威资料（不可虚构，无依据必须说明）：
[疾病-处方检索结果] ...
[中医典籍检索结果] ...

任务：基于基础方剂，结合猪只参数（体重/月龄/病情严重度）加减化裁，输出严格JSON：
{
  "base_formula": "基础方剂名",
  "herbs": [{"name": "药材", "dosage_g": 15, "role": "君/臣/佐/使"}],
  "add_notes": ["加减说明"],
  "preparation": "用法（按500g饲料混饲）",
  "course": "疗程",
  "contraindications": ["禁忌"],
  "references": ["引用出处"]
}
```

**阶段三：校验Agent（规则引擎，非LLM，确定性规则）**

```
校验项：
  1. 剂量是否在药材安全区间内（对照药材属性表）
  2. 配伍禁忌（十八反/十九畏查表）
  3. 特殊状态（孕猪/幼龄）禁用项
通过 → 输出；不通过 → 标记并让 LLM 修正一次，仍不通过则打回人工审核
```

> 校验Agent = 硬性安全护栏（毒性/配伍禁忌规则表），不依赖 LLM 判断，确保用药绝对安全。

> 剂量换算：`混饲剂量 = 基础剂量 × 体重系数(0.7/1.0/1.3/1.5) × 病情系数(0.8/1.0/1.2)`

#### 3.2.5 SFT 微调（可选，非必需）

按本板块定调，**辨证组方依赖 RAG + 现成 LLM，不依赖模型训练**。SFT 仅作为效果不足时的**可选优化**：

- 触发条件：专家评估辨证合理率 < 85% 且 RAG 检索已调优仍不足
- 微调数据：兽医专家标注的 {症状+体征} → {证型+组方} 对（200-500条即可）
- 微调方式：LoRA，rank=16, alpha=32（百炼平台 SFT）
- 评估：留出20%数据，BLEU/ROUGE + 兽医专家人工评分

---

### 3.3 治疗方案生成（Treatment Generation）

#### 3.3.1 疾病-处方库（核心，结构化表 + 向量化）

治疗方案的核心数据源是**疾病-处方库**（结构化表，CSV/MySQL 存储 + 向量化入 RAG），**不依赖模型训练**：

**表结构**：

| 字段 | 说明 | 示例 |
|------|------|------|
| disease | 疾病/证型 | 疫热壅肺（中期） |
| base_formula | 基础方剂 | 麻杏石甘汤加减 |
| herbs | 药材+基础剂量 | 麻黄15g；石膏30g；杏仁15g；甘草10g |
| add_rule | 加减规则 | 痰多加桔梗5g、半夏10g；气虚加黄芪30g；久咳加满山红5g |
| usage | 用法 | 按500g饲料混饲，连用5-7天 |
| contraindication | 禁忌 | 孕猪慎用麻黄；表虚自汗者忌 |
| reference | 出处 | 《中兽医学》/ 临床病例 RX-xxx |

**种子数据（首批录入，PPT已验方剂优先）**：

| 证型 | 基础方剂 | 来源 |
|------|---------|------|
| 中期·疫热壅肺 | 黄芪30g 甘草25g 黄芩20g 茯苓15g 桔梗5g 满山红5g | PPT三代已验证组方 |
| 前期·邪袭肺卫 | 银翘散加减（金银花、连翘、桔梗、薄荷...） | 中医典籍 |
| 后期·气阴两伤 | 生脉散加减（党参、麦冬、五味子...）+扶正药 | 中医典籍 |
| 痰热壅肺 | 清肺止咳散加减 | 中医典籍 |

#### 3.3.2 组方生成流水线（RAG 驱动）

```
辨证结果 ──▶ 疾病-处方库检索 ──▶ 基础方剂匹配 ──▶ LLM加减化裁 ──▶ 剂量计算 ──▶ 规则校验 ──▶ 处方输出
   │              │                    │                │              │            │
   │              ▼                    ▼                ▼              ▼            ▼
   │        向量检索Top3          按匹配度排序       Qwen推理      按体重/病情     十八反十九畏
   │        (或关键词匹配)         含加减规则       个性化调整      换算剂量        安全区间检查
```

#### 3.3.3 中药知识图谱（口径说明）

探讨预期中"构建高质量中药知识图谱"即本模块的**两个核心库（辨证库 + 论治库）及其理-法-方-药关系链**——以结构化表（CSV/MySQL）+ 向量化（ChromaDB）形式实现，这是知识图谱的工程化轻量落地，实体与关系完整可查询。

- **可选增强**：时间充裕时用 Neo4j 将"证候→方剂→药材"关系图可视化（ECharts 展示），仅作演示增强，不参与核心推理
- 交付口径：**"中药知识图谱"交付物 = 辨证库 + 论治库 + 理法方药逻辑链**（结构化 + 向量化），Neo4j 图为加分项

#### 3.3.4 剂量计算模型

基于猪只体重和给药途径的剂量换算：

```
混饲剂量 (g/500g饲料) = 标准剂量 × 体重系数 × 病情系数

体重系数:
  - 保育猪 (5-25kg):   0.7
  - 生长猪 (25-60kg):   1.0  (基准)
  - 育肥猪 (60-100kg):  1.3
  - 母猪 (>100kg):      1.5

病情系数:
  - 轻度 (初期、单咳):  0.8
  - 中度 (中期、连咳):  1.0
  - 重度 (后期、呼吸困难): 1.2
```

---

### 3.4 评估反馈闭环

#### 3.4.1 治疗反馈追踪

```json
{
  "prescription_id": "RX-20260804-001",
  "pig_house": "第8圈第6舍",
  "treatment_start": "2026-08-04",
  "follow_ups": [
    {
      "day": 3,
      "cough_frequency_change": "-40%",
      "temperature_change": "-0.8°C",
      "mental_state": "改善",
      "adjustment": "继续原方"
    },
    {
      "day": 7,
      "cough_frequency_change": "-85%",
      "temperature_change": "恢复正常",
      "mental_state": "正常",
      "adjustment": "转为巩固方，减量1/3"
    }
  ],
  "outcome": "治愈",
  "effective_score": 0.95
}
```

#### 3.4.2 持续学习机制

- **处方效果数据积累** → 反馈更新疾病-处方库与中医典籍库
- **有效方剂-证型关联强化** → 提升对应记录检索权重
- **治疗效果差的案例** → 人工复盘，更新处方库加减规则

---

## 四、技术栈总览

### 4.1 核心技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| **基座模型** | Qwen-Max / Qwen-Plus / Qwen-Turbo | 百炼平台 API 调用 |
| **模型部署** | 阿里云百炼平台 | 模型 API（SFT 微调可选） |
| **向量数据库** | ChromaDB（本地零配置）/ Milvus | RAG 知识检索 |
| **结构化数据** | MySQL / CSV | 症状-疾病库、疾病-处方库 |
| **图数据库** | Neo4j（可选） | 仅演示可视化，非依赖 |
| **深度学习框架** | PyTorch 2.x / TensorFlow 2.x | 诊断模型训练 |
| **音频处理** | librosa, python_speech_features | 声学特征提取 |
| **后端框架** | Spring Boot 3.x | API 服务 |
| **数据库** | MySQL 8.0 + Redis | 业务数据 + 缓存 |
| **前端** | 微信小程序 + ECharts | 诊断结果可视化 |
| **容器化** | Docker + Docker Compose | 环境一致性 |
| **版本控制** | Git + Gitee/GitHub | 代码管理 |
| **音频工具** | Audacity / Adobe Audition | 样本标注 |

### 4.2 开发环境

| 组件 | 配置 |
|------|------|
| GPU | NVIDIA RTX 3060/3080/4060 |
| Python | 3.10+ |
| CUDA | 11.8 / 12.1 |
| OS | Linux (Ubuntu 20.04/22.04) 或 Windows 11 + WSL2 |
| IDE | VS Code / PyCharm |

---

## 五、实施步骤与时间节点

### 5.1 15天实施计划

```
Day 0  ── 技术路线确认，环境搭建，百炼API开通
Day 3  ── 诊断数据库 + 三大知识库结构定稿，开始数据采集
Day 6  ── 诊断模型v1训练完成（Baseline），三库首批数据入库
Day 9  ── RAG检索 + LLM辨证组方流水线跑通
Day 12 ── 治疗组方联调 + 规则校验完成，端到端测试
Day 15 ── 系统集成联调，辨病+论治全链路验证，撰写技术文档
```

### 5.2 详细任务分解

| 阶段 | 任务 | 负责人 | 产出物 |
|------|------|--------|--------|
| **Day 0-3 基础搭建** | | | |
| | 百炼平台API开通与测试 | 阴学舟 | API凭证 + 调用Demo |
| | Qwen模型选型与初步测试 | 阴学舟 | 模型对比报告 |
| | 诊断数据库Schema设计 | 刘宇翔 | ER图 + DDL |
| | 三大知识库结构设计（症状-疾病/疾病-处方/典籍） | 阴学舟 | 库结构文档 |
| | 文献/方剂数据收集整理 | 柴堰颖 | 原始数据集 |
| **Day 3-6 诊断模型** | | | |
| | 声学特征提取pipeline搭建 | 刘宇翔 | Python模块 |
| | 诊断分类模型Baseline训练 | 刘宇翔 | 模型文件 + 评估报告 |
| | 多模态特征对齐实验 | 阴学舟 | 实验结果 |
| | 集成学习优化（SSLRB） | 阴学舟 | 优化后模型 |
| **Day 6-9 论治引擎（RAG）** | | | |
| | 三库数据入库 + 向量化 | 柴堰颖 | 导入脚本 + 向量库 |
| | RAG检索pipeline搭建 | 阴学舟 | 检索API |
| | LLM辨证组方两阶段Prompt开发 | 阴学舟 | Prompt模板 + 调用API |
| | 检索质量联调测试 | 刘宇翔 | 测试用例 + 结果 |
| **Day 9-12 治疗生成** | | | |
| | 组方生成API完善 | 阴学舟 | 组方API |
| | 剂量计算模块 | 刘宇翔 | 计算函数 + 测试 |
| | 规则校验引擎（禁忌/剂量区间） | 刘宇翔 | 校验规则 + API |
| | 前端诊断页面开发 | 柴堰颖 | 小程序页面 |
| **Day 12-15 集成联调** | | | |
| | 诊断→辨证→组方全链路联调 | 全员 | 端到端测试报告 |
| | API接口文档编写 | 阴学舟 | API文档 |
| | 技术文档撰写 | 全员 | 技术报告 |
| | 演示视频录制 | 柴堰颖 | 演示视频 |

---

## 六、实验设计

### 6.1 诊断模型实验

**数据集**：
- 训练集：已标注咳嗽+疾病标签的声音样本（目标 ≥500条/类）
- 金标准确诊样本 5%-20%（经2名以上兽医独立审核）
- 8:1:1 训练/验证/测试划分，分层抽样保证类别平衡

**Baselines**：

| 方法 | 来源 | 预期性能 |
|------|------|---------|
| SVM (RBF核, MFCC特征) | 谢杰(2023) | ~87% |
| LSTM (MFCC特征) | 谢杰(2023) | ~90% |
| CNN-BiLSTM (MFCC特征) | 付小朋(2025) | ~84% |
| CNN (logFBank特征) | 王梦雨(2022) | ~96% (二分类) |
| MFCC-CNNs (MFCC+差分) | 王梦雨(2022) | ~93% (四分类) |
| A-TF 多域融合 | 纪楠(2023) | ~97% (二分类) |
| **本方案 (A-TF + SSLRB)** | 本文 | **目标 ≥90% (多分类)** |

**Metrics**：
- 总体准确率 (Accuracy)
- 各类别精确率/召回率/F1
- 宏平均F1 (Macro-F1)
- 加权F1 (Weighted-F1)
- 混淆矩阵
- PR-AUC（评估不平衡鲁棒性）

### 6.2 辨证推理实验

**评估维度**：

| 指标 | 方法 | 目标 |
|------|------|------|
| 证型判定准确率 | 与兽医专家标注对比 | ≥85% |
| 分期判定准确率 | 与兽医专家标注对比 | ≥90% |
| 推理逻辑一致性 | 兽医专家评分 (1-5) | ≥4.0 |
| 处方合理率 | 兽医专家审核通过率 | ≥85% |
| 生成引用准确率 | 文献/方剂出处是否真实可查 | 100%（严禁虚构） |

**Baselines**：
- 无RAG的Qwen直接推理（基线，检验检索增益）
- 基于规则的模板匹配（非LLM方案）

### 6.3 治疗生成实验

**评估指标**：
- 方剂-证型匹配准确率
- 剂量安全范围合规率（目标 100%）
- 配伍禁忌检测召回率（目标 100%）
- 兽医专家对处方质量的 Likert 5级评分

---

## 七、风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| 标注数据不足（多分类标签） | 高 | 诊断模型性能受限 | ①迁移学习（预训练咳嗽识别模型微调）②小样本学习/数据增强 ③主动学习迭代标注 |
| Qwen模型幻觉（虚构文献/方剂） | 中 | 处方安全风险 | ①RAG强制约束引用真实来源 ②规则校验引擎二次校验 ③输出格式约束（JSON Schema）④无检索命中时降级标注 |
| 三库数据不足/结构化困难 | 中 | 检索覆盖面不全 | ①优先手动整理核心数据（20-30首方剂、常见症状-疾病对）②逐步扩展 ③利用Qwen辅助文献信息抽取 |
| 百炼API调用延迟/限流 | 低 | 实时性不足 | ①Redis缓存常见诊断结果 ②异步推理+结果轮询 ③本地部署Qwen-Turbo作为降级方案 |
| 团队对中医理论不熟悉 | 高 | 辨证逻辑质量差 | ①与兽医专家团队（殷中琼等）紧密协作 ②专家审核检索库与辨证输出 ③PPT三代技术已验证的组方作为种子数据 |

---

## 八、预期成果

### 8.1 交付物

| 序号 | 交付物 | 形式 |
|------|--------|------|
| 1 | 诊断分类模型（训练权重 + 推理代码） | Python模块 + .pth/.h5文件 |
| 2 | 辨证组方引擎（LLM两阶段 + RAG检索） | Python模块 + API |
| 3 | 三大知识库（症状-疾病/疾病-处方/中医典籍） | CSV/MySQL + 向量库 |
| 4 | RAG检索管线（向量嵌入 + 检索API） | ChromaDB + API |
| 5 | 治疗组方生成API（含剂量计算 + 规则校验） | REST API |
| 6 | 技术文档（架构设计 + 实验报告） | PDF + Markdown |
| 7 | 演示视频 | MP4 (≤10min) |
| 8 | 小程序诊疗决策页面 | 微信小程序页面 |

### 8.2 创新点

1. **首次将大语言模型（Qwen）+ RAG 应用于猪呼吸道疾病的中兽医辨证论治**，实现从"声学检测"到"智能诊疗"的闭环
2. **两层认知模型架构**：辨病层（轻量多分类，数据要求低）+ 论治层（LLM+RAG，无需训练），分工清晰、可独立演进
3. **RAG增强的中兽医知识推理**：症状-疾病/疾病-处方/中医典籍三库检索，解决LLM幻觉问题，确保处方基于真实文献和临床数据，推理链可追溯
4. **轻量落地**：诊断与组方均不依赖大规模数据训练，冷启动快，适配中小规模猪场场景
5. **诊断模型层面**：继承并改进纪楠(2023) A-TF多域融合+SSLRB集成学习，适配多分类和不平衡场景

---

## 附录：参考资料

1. 纪楠. 基于多域特征融合的生猪咳嗽声识别方法研究[D]. 东北农业大学, 2023.
2. 付小朋等. 基于CNN-BiLSTM的猪咳嗽声识别方法[J]. 河南农业科学, 2025.
3. 王锡鹏. 复杂环境下基于多分类器融合的猪咳嗽声自动检测方法研究[D]. 东北农业大学, 2024.
4. 王梦雨. 基于深度神经网络的猪只咳嗽声识别预警系统设计与实现[D]. 南京农业大学, 2022.
5. 谢杰. 基于智能语音技术的猪只声音监测研究[D]. 2023.
6. XH-202619. 基于国产开源大模型的AI Scientist的研发与应用（挑战杯赛题方案）.
7. 音甘猪咳嗽小挑校赛三代技术PPT. 2025.
8. 音甘猪咳嗽声智能监测与诊疗辅助决策系统项目方案. 2026.

## 附录：配套文档导航

| 文档 | 内容 |
|------|------|
| 诊疗决策板块_实操手册.md | 环境/数据/模型/RAG/联调全流程实操 |
| 诊疗决策板块_数据库设计.md | 12 表设计、ER、数据字典 |
| 诊疗决策板块_运行流程说明.md | 系统全景、五步链路、部署、排障 |
| 诊疗决策板块_测试手册.md | 测试样例、预期结果、验收 |
| 诊疗决策板块_开发日志.md | 开发时间线、决策记录、完成清单 |
