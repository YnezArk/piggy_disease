# 猪咳嗽AI辅助诊疗系统 — 数据库整合版说明文档

> **版本**：v2.0（整合版）  
> **日期**：2026-08-13  
> **数据库**：pig_diag（MySQL 5.7+）  
> **字符集**：utf8mb4


## 一、致模型组的同事：数据库变更说明

### 1.1 变更概述

本次将原 `pig_cough` 数据库与模型组标准结构（`pig_diag`）进行了**整合迁移**。整合后统一使用 `pig_diag` 作为最终数据库。

### 1.2 对模型组的影响

| 影响项          | 说明                                                         |
| --------------- | ------------------------------------------------------------ |
| **表名/字段名** | ❌ 未变更，保持原 `pig_diag` 标准结构，模型代码**零修改**     |
| **数据内容**    | ✅ 已整合原 `pig_cough` 中所有知识数据和声学特征数据          |
| **连接配置**    | ⚠️ 需要将数据库连接指向 `pig_diag`（原 `pig_cough` 可保留作为备份） |
| **代码改动**    | ❌ 无需任何代码改动                                           |

### 1.3 一句话总结

> 模型组只需要把数据库连接从 `pig_cough` 换成 `pig_diag`，其他一切照旧。


## 二、快速部署指南（3步上手）

### 第1步：导入数据库

```bash
# 在MySQL中创建数据库
CREATE DATABASE IF NOT EXISTS pig_diag DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入数据（命令行）
mysql -u root -p pig_diag < pig_diag_v1.sql
```

或用Navicat：新建数据库 → 右键“运行SQL文件” → 选择 `pig_diag_v1.sql`

### 第2步：修改连接配置

将项目中数据库连接改为：
```
host: localhost
port: 3306
database: pig_diag
user: （你的数据库用户名）
password: （你的数据库密码）
```

### 第3步：验证数据完整性

```sql
USE pig_diag;

SELECT 'disease' AS table_name, COUNT(*) FROM disease
UNION ALL SELECT 'syndrome', COUNT(*) FROM syndrome
UNION ALL SELECT 'herb', COUNT(*) FROM herb
UNION ALL SELECT 'formula', COUNT(*) FROM formula
UNION ALL SELECT 'formula_herb', COUNT(*) FROM formula_herb
UNION ALL SELECT 'diagnosis_record', COUNT(*) FROM diagnosis_record
UNION ALL SELECT 'acoustic_feature', COUNT(*) FROM acoustic_feature;
```

预期结果：

| table_name       | COUNT(*) |
| ---------------- | -------- |
| disease          | 6        |
| syndrome         | 8        |
| herb             | 88       |
| formula          | 22       |
| formula_herb     | 130      |
| diagnosis_record | 198      |
| acoustic_feature | 792      |


## 三、数据库整体结构

```
pig_diag
├── 📁 知识底座（模型组标准表，代码不改）
│   ├── disease              # 疾病字典（含别名、模型标签）
│   ├── syndrome             # 证候字典（含治则治法）
│   ├── syndrome_mapping     # 辨证规则库（体征→证候映射）
│   ├── herb                 # 药材库（性味归经+剂量区间）
│   ├── formula              # 方剂表
│   ├── formula_herb         # 方剂-药材关联表
│   ├── herb_contraindication # 配伍禁忌（十八反十九畏）
│   ├── add_rule             # 随症加减规则表
│   └── kg_source            # 知识来源表
│
├── 📁 业务记录（模型组标准表）
│   ├── diagnosis_record     # 辨病记录（诊断结果）
│   ├── prescription_record  # 处方记录
│   └── treatment_feedback   # 疗效反馈
│
├── 📁 声学特征（原pig_cough独有，已整合）
│   ├── acoustic_feature     # 声学特征向量（MFCC/logFBank/时域/频域）
│   └── multimodal_feature   # 多模态特征（体温/行为/环境）
│
├── 📁 养殖管理（原pig_cough独有，已整合）
│   ├── pig_farms            # 养殖场
│   ├── pig_groups           # 猪群
│   ├── pigs                 # 个体猪只
│   └── users                # 用户
│
├── 📁 农户指导（原pig_cough独有，已整合）
│   ├── farmer_guidance      # 农户指导建议
│   └── guidance_execution   # 指导执行反馈
│
├── 📁 系统支撑（原pig_cough独有，已整合）
│   ├── model_version        # 模型版本管理
│   ├── training_dataset     # 训练数据集管理
│   ├── agent_log            # Agent运行日志
│   ├── system_config        # 系统配置
│   └── dashboard_stats      # 统计看板
│
└── 📁 用户关联
    └── user_group           # 用户-猪群关联表
```


## 四、核心数据表详解（模型训练必读）

### 4.1 疾病字典：`disease`

```sql
SELECT id, name, alias, label FROM disease;
```

| id   | name               | alias                     | label      |
| ---- | ------------------ | ------------------------- | ---------- |
| 1    | 健康猪只           | 猪气喘病,支原体肺炎,Mhp   | mycoplasma |
| 2    | 猪流行性感冒       | 猪流感,SI,Swine Influenza | influenza  |
| 3    | 猪蓝耳病           | 蓝耳病,PRRS               | prrs       |
| 4    | 猪支原体肺炎       | PRDC,多病原混合感染       | mixed      |
| 5    | 猪传染性胸膜肺炎   | APP,胸膜肺炎放线杆菌病    | app        |
| 6    | 猪繁殖与呼吸综合征 | 蓝耳病,猪蓝耳病,PRRS,...  | prrs       |

> **注意**：`alias` 字段存别名（逗号分隔），`label` 为辨病模型分类标签。

### 4.2 辨病记录：`diagnosis_record`（⭐ 模型输出落表）

```sql
CREATE TABLE diagnosis_record (
    id              BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT,
    pig_house       VARCHAR(64) COMMENT '猪舍编号',
    model_label     VARCHAR(64) COMMENT '辨病模型输出标签',
    disease_id      BIGINT UNSIGNED COMMENT '命中疾病',
    confidence      DECIMAL(4,3) COMMENT '置信度0-1',
    temp_c          DECIMAL(4,1) COMMENT '体温℃',
    mental_state    VARCHAR(64) COMMENT '精神状态',
    env_json        JSON COMMENT '环境参数快照',
    feature_json    JSON COMMENT '声学特征快照(可选)',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**当前数据量**：198条（record_id 371-568）

### 4.3 声学特征：`acoustic_feature`（⭐ 模型输入特征）

| 字段              | 类型        | 说明                                  |
| ----------------- | ----------- | ------------------------------------- |
| feature_id        | INT         | 主键                                  |
| diagnosis_id      | BIGINT      | 关联 `diagnosis_record.id`            |
| feature_type      | VARCHAR(30) | MFCC / logFBank / 时域特征 / 频域特征 |
| feature_dimension | INT         | 特征维度                              |
| feature_vector    | JSON        | 特征向量（数值数组）                  |

**特征说明**：

| feature_type | 维度 | 说明                       |
| ------------ | ---- | -------------------------- |
| MFCC         | 13   | 梅尔频率倒谱系数           |
| logFBank     | 64   | 对数梅尔频谱               |
| 时域特征     | 3    | 能量、过零率、RMS          |
| 频域特征     | 4    | 质心、带宽、平坦度、滚降点 |

**每条诊断记录对应4条特征记录，拼接后总维度 = 13+64+3+4 = 84维**

### 4.4 药材库：`herb`

```sql
SELECT id, name, property, flavor, meridian, dosage_min, dosage_max FROM herb LIMIT 5;
```

| id   | name | property | flavor   | meridian | dosage_min | dosage_max |
| ---- | ---- | -------- | -------- | -------- | ---------- | ---------- |
| 1    | 麻黄 | 温       | 辛、微苦 | 肺、膀胱 | 3.0        | 10.0       |
| 2    | 石膏 |          | 辛、甘   | 肺、胃   | 15.0       | 60.0       |
| 3    | 杏仁 | 温       | 苦       | 肺、大肠 | 5.0        | 15.0       |

### 4.5 方剂与组成：`formula` + `formula_herb`

`formula` 存方剂主表，`formula_herb` 存方剂-药材关联：

```sql
-- 查询麻杏石甘汤的组成
SELECT f.name, h.name AS herb_name, fh.dosage_g, fh.role
FROM formula f
JOIN formula_herb fh ON f.id = fh.formula_id
JOIN herb h ON fh.herb_id = h.id
WHERE f.name = '麻杏石甘汤'
ORDER BY fh.sort_order;
```

### 4.6 辨证规则：`syndrome_mapping`

多维体征 → 证候映射规则，支持体温区间、咳嗽类型、排泄物等多条件组合查询。

| 字段                | 说明           |
| ------------------- | -------------- |
| temperature_min/max | 体温区间       |
| cough_type          | 咳喘表现关键词 |
| excretion           | 排泄物观察     |
| other_signs         | 其他体征       |
| syndrome_id         | 命中证候       |
| disease_id          | 关联疾病       |
| evidence            | 鉴别依据       |
| weight              | 权重1-10       |


## 五、表关系图（核心）

```
                    ┌─────────────────┐
                    │    disease      │
                    │  (疾病标签)      │
                    └────────┬────────┘
                             │ 1:N
                             ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│ acoustic_feature │───│diagnosis_record │───│   pig_groups    │
│  (特征向量 X)     │    │  (样本主表)     │    │  (猪群)         │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                               │
                               │ 1:N（后续AI写入）
                               ▼
                    ┌─────────────────┐
                    │prescription_record│
                    │  (处方记录)      │
                    └─────────────────┘
                               │
                               ▼
                    ┌─────────────────┐
                    │treatment_feedback│
                    │  (疗效反馈)      │
                    └─────────────────┘
```

**核心关联路径**：
```
acoustic_feature.diagnosis_id → diagnosis_record.id
diagnosis_record.disease_id → disease.id
prescription_record.diagnosis_id → diagnosis_record.id
```


## 六、模型训练数据读取指南

### 6.1 读取训练数据（SQL模板）

```sql
-- 构建训练数据集：特征 + 标签
SELECT 
    d.id AS diagnosis_id,
    d.disease_id,
    d.model_label,
    MAX(CASE WHEN a.feature_type = 'MFCC' THEN a.feature_vector END) AS mfcc,
    MAX(CASE WHEN a.feature_type = 'logFBank' THEN a.feature_vector END) AS logfbank,
    MAX(CASE WHEN a.feature_type = '时域特征' THEN a.feature_vector END) AS temporal,
    MAX(CASE WHEN a.feature_type = '频域特征' THEN a.feature_vector END) AS spectral
FROM diagnosis_record d
JOIN acoustic_feature a ON d.id = a.diagnosis_id
WHERE d.id >= 371
GROUP BY d.id, d.disease_id, d.model_label;
```

### 6.2 Python读取示例

```python
import pymysql
import json
import numpy as np

conn = pymysql.connect(
    host='localhost', user='root', password='your_pw',
    database='pig_diag', charset='utf8mb4'
)

sql = """
SELECT d.id, d.disease_id, a.feature_type, a.feature_vector
FROM diagnosis_record d
JOIN acoustic_feature a ON d.id = a.diagnosis_id
WHERE d.id >= 371
"""
cursor = conn.cursor()
cursor.execute(sql)

data = {}
for diagnosis_id, disease_id, feat_type, feat_json in cursor.fetchall():
    if diagnosis_id not in data:
        data[diagnosis_id] = {'disease_id': disease_id, 'features': {}}
    data[diagnosis_id]['features'][feat_type] = json.loads(feat_json)

X, y = [], []
for diagnosis_id, item in data.items():
    feat = np.concatenate([
        np.array(item['features']['MFCC']),
        np.array(item['features']['logFBank']),
        np.array(item['features']['时域特征']),
        np.array(item['features']['频域特征'])
    ])
    X.append(feat)
    y.append(item['disease_id'] - 1)  # 0-based label

X = np.array(X)  # shape: (198, 84)
y = np.array(y)  # shape: (198,)

print(f"特征矩阵: {X.shape}, 标签: {y.shape}")
```


## 七、当前数据规模

| 表               | 数据量 | 说明                |
| ---------------- | ------ | ------------------- |
| disease          | 6      | 5种疾病 + 1健康     |
| syndrome         | 8      | 证型字典            |
| syndrome_mapping | 11     | 辨证规则            |
| herb             | 88     | 药材库              |
| formula          | 22     | 方剂模板            |
| formula_herb     | 130    | 方剂-药材关联       |
| diagnosis_record | 198    | 有效诊断记录        |
| acoustic_feature | 792    | 声学特征（198 × 4） |
| kg_source        | 13     | 知识来源            |
| pig_farms        | 1      | 测试养殖场          |
| pig_groups       | 3      | 测试猪群            |
| users            | 1      | 测试用户            |


## 八、空表说明（AI运行时自动写入）

以下表当前为空，由后续AI模型/Agent在运行时自动写入：

| 表名                  | 写入者    | 触发时机       |
| --------------------- | --------- | -------------- |
| `prescription_record` | 组方Agent | 辨证完成后     |
| `treatment_feedback`  | 系统      | 疗效追踪时     |
| `farmer_guidance`     | AI/兽医   | 生成指导建议时 |
| `guidance_execution`  | 农户      | 执行反馈时     |
| `agent_log`           | 各Agent   | 每次运行       |
| `model_version`       | 系统      | 模型部署时     |
| `training_dataset`    | 系统      | 数据集构建时   |


## 九、验证清单

导入数据库后执行以下验证：

```sql
USE pig_diag;

-- 1. 表数量（应为34张表）
SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA = 'pig_diag';

-- 2. 核心数据量
SELECT 'disease' AS tbl, COUNT(*) FROM disease
UNION ALL SELECT 'syndrome', COUNT(*) FROM syndrome
UNION ALL SELECT 'herb', COUNT(*) FROM herb
UNION ALL SELECT 'formula', COUNT(*) FROM formula
UNION ALL SELECT 'formula_herb', COUNT(*) FROM formula_herb
UNION ALL SELECT 'diagnosis_record', COUNT(*) FROM diagnosis_record
UNION ALL SELECT 'acoustic_feature', COUNT(*) FROM acoustic_feature;

-- 3. 各疾病样本分布
SELECT d.name, COUNT(r.id) AS samples
FROM diagnosis_record r
JOIN disease d ON r.disease_id = d.id
WHERE r.id >= 371
GROUP BY r.disease_id
ORDER BY samples DESC;
```


## 十、常见问题

**Q1：为什么 diagnosis_record.id 从371开始？**  
A：早期手动插入的测试数据（1-370）没有对应的声学特征，已排除。有效训练数据从371开始。

**Q2：acoustic_feature 的 feature_vector 怎么查看具体数值？**  
A：Navicat 17+ 双击单元格可查看JSON内容；或使用 `JSON_EXTRACT(feature_vector, '$[0]')` 提取单个值。

**Q3：新数据如何入库？**  
A：音频 → librosa提取特征 → 插入 `diagnosis_record` → 插入 `acoustic_feature`（参考 `extract_and_insert.py`）。

**Q4：model_label 和 label 有什么区别？**  
A：`disease.label` 是疾病的标准分类标签（英文小写），供模型训练使用；`diagnosis_record.model_label` 是模型实际输出的标签值。

**Q5：原 pig_cough 还需要保留吗？**  
A：建议保留作为备份，待确认 `pig_diag` 运行稳定后可删除或归档。


## 十一、快速连接信息

| 配置项   | 值         |
| -------- | ---------- |
| 数据库名 | `pig_diag` |
| 字符集   | `utf8mb4`  |
| 端口     | `3306`     |
| 时区     | 系统默认   |

*文档结束*