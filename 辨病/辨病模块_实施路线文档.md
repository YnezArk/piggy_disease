# 辨病模块 — 实施路线文档

> **项目**：音甘猪咳嗽声智能监测与诊疗辅助决策系统
> **板块**：板块4 — 诊疗决策（辨病 + 辨证组方）| 决策部分已完成，本文规划辨病部分
> **负责人**：阴学舟（主责）、刘宇翔、柴堰颖
> **日期**：2026-08-13
> **基座模型**：Qwen（百炼网关，`qwen3.7-plus`）| 数据环境：Python 3.14 / librosa / MySQL 5.7（pig_cough）

---

## 一、模块定位与接口契约

### 1.1 在决策板块中的位置

决策板块采用**两层认知模型**：辨病层（轻量多分类，本模块）→ 论治层（LLM+RAG，已完成，见 `therapy_engine.py`）。

```
监测板块(咳嗽检测) ──▶ [辨病层·本模块] 疾病多分类 ──▶ [论治层·已完成] 辨证组方 ──▶ 输出
                            │                              ▲
                            │  disease + confidence         │ disease_conflict 纠错
                            └───────────────────────────────┘
```

### 1.2 与论治层的接口契约（关键）

论治引擎已就绪，辨病模块必须按契约喂数据，否则链路不通：

| 契约字段 | 类型 | 说明 | 论治层消费点 |
|---------|------|------|-------------|
| `disease` | str | 疾病中文名（须过别名归一） | `therapy_engine` 辨病纠错、辨证 Agent |
| `confidence` | float | 模型置信度 | API 落库 `diagnosis_record.confidence` |
| `temp_c` | float | 体温（多模态，可空） | 辨证库 SQL 精确检索 |
| `typical_symptoms` | str | 典型症状模板，**仅输出展示，不输入论治** | 前端显示 |
| `symptoms` | str | 实际症状，**手动输入**（2026-08-16 定调，不再用辨病模板兜底） | 辨证 Agent 输入 |
| `cough_type` | str | 咳声类型 | 辨证库匹配 |

- **疾病名必须与论治层 `disease` 表别名归一一致**：猪蓝耳病≡猪繁殖与呼吸综合征（`kb_lookup._same_disease`），避免辨病纠错误报
- 论治层已有**辨病纠错兜底**（规则交叉验证 + LLM 独立判断）——辨病模型出错不会直接导致错误处方，但不应依赖此兜底掩盖模型缺陷
- 桥接代码：`诊疗决策板块_实操手册.md` §bridge.py 草稿，本模块落地为正式 `bridge.py`

### 1.3 数据库现状（单库 pig_diag_v2）

原 pig_cough（辨病训练数据）与 pig_diag（论治知识库）已整合为 **`pig_diag_v2` 单库**（26 表，建库脚本 `pig_diag_v2.sql`，见《诊疗决策板块_数据库设计_v2.0.md》）。本模块相关表：

| 分组 | 表 | 用途 |
|------|----|------|
| 声学特征 | `acoustic_feature` | 特征向量（⭐ 模型输入 X，198 行，每样本一行 84 维） |
| 业务记录 | `diagnosis_record` | 训练样本主表（198 条，disease_id 为标签） |
| 业务记录 | `prescription_record` | 论治结果回写 |
| 知识底座 | `disease` | 疾病字典（label 与标注文件夹一致，含 7 病） |
| 系统支撑 | `model_version` / `training_dataset` | 模型版本、数据划分落库 |

- 连接配置：`.env` 的 `DB_NAME=pig_diag_v2`（config.py 统一读取，代码零修改）
- ⚠️ 旧库命名（pig_cough 的 `diagnosis_records`/`acoustic_features` 复数表名）已弃用，SQL 模板按 v2 单数表名，见 v2.0 设计文档 §9

---

## 二、现状盘点（已具备资产）

### 2.1 标注音频数据

`辨病/pig_cough_data/`，**2026-08-17 再次扩充**：共 **540 个标注样本**（6 类，无 unknown）。文件夹名与 `pig_diag_v2.disease.label` 对齐（normal/influenza/prrs/mycoplasma/app/other_disease；子类结构已展平）：

| 类别 | 目录名（= DB label） | 样本数 | 说明 |
|------|--------|-------|------|
| 健康猪只 | normal | 167 | 含 7 子类展平：environment 5 / hungry 8 / oink 25 / play 10 / scream 7 / snore 5 / unknown_normal 107 |
| 猪流行性感冒 | influenza | 135 | 含 gander（鹅鸣样咳）12 |
| 猪支原体肺炎 | mycoplasma | 148 | myco_*.wav |
| 猪蓝耳病 | prrs | 28 | prrs_*.wav |
| 猪传染性胸膜肺炎 | app | 49 | app_*.wav |
| **其他疾病** | **other_disease** | **13** | rhinobyon_*.wav，2026-08-17 纳入第 6 类（DB disease 已补行） |

**音频规格**：8000Hz/1.0s 与 44100Hz/0.3~2.2s 并存（librosa 自动重采样 16k + 截断/填充至 1.0s）。旧数据集备份于 `pig_cough_data_old/`。

### 2.2 已入库特征（`pig_diag_v2` 库）

`extract_and_insert（提取特征的脚本）.py` 已完成首轮入库，数据已整合迁移至 `pig_diag_v2`：

- `diagnosis_record`：540 条（id 870-1409，2026-08-17 六类数据集重建）
- `acoustic_feature`：540 行（宽表，每样本一行，mfcc/logfbank/temporal/spectral 四列并列）
- 特征构成：**MFCC 13 + logFBank 64 + 时域 3 + 频域 4 = 84 维**（均值池化）
- 提取参数：`sr=16000`（源音频 8k 自动重采样）/ `duration=1.0s` / `n_fft=2048` / `hop=512`
- 标签 ground truth：disease_id 分布 1=167 / 2=135 / 3=28 / 4=148 / 5=49 / 6=13，对应 {1:normal, 2:influenza, 3:prrs, 4:mycoplasma, 5:app, 6:other_disease}（映射约定见 v2.0 设计文档 §11.1）
- 数据备份：`pig_diag_v2.sql`（全量可重建，已重新导入验证）
- 表结构文档：《诊疗决策板块_数据库设计_v2.0.md》

### 2.3 既有路线基础

《诊疗决策板块_技术路线报告.md》§3.1 已定：
- 主方案 A：**多域特征融合 + SSLRB 集成**（SVM-Stacking 3 基分类器 + LR-Bagging + ROS 上采样），纪楠(2023) 重度不平衡 F1=92.19%
- 备选方案 B：CNN-BiLSTM 多分类（付小朋 2025 扩展）
- 进阶方案 C：多模态对齐融合（需体温/行为数据，暂缓）
- 目标：多分类准确率 ≥ 90%（需按小样本现实修正为 F1 导向，见 §5.3）

---

## 三、差距分析（现状 vs 目标）

| # | 问题 | 影响 | 对策 |
|---|------|------|------|
| 1 | **样本量小**：病类仅 13~14 条/类（文献目标 ≥500/类） | 模型方差大、易过拟合、评测不可靠 | ①数据增强 ②分层5折CV+固定划分 ③预训练特征迁移 ④持续采集回路 |
| 2 | **类别极不平衡**：normal 54% vs 病类 6.6~7.1% | 准确率虚高、病类召回低 | ①SSLRB+ROS 上采样 ②Macro-F1 为主指标 ③class_weight |
| 3 | **标签可靠度未知**：目录名即标签，无兽医双审、无金标准确诊样本 | 噪声标签污染模型 | ①柴堰颖人工抽审（听辨+文档溯源）②unknown 2 条不入选 ③标签审计表落库 |
| 4 | **特征管线不一致**：库中 84 维均值池化 vs 手册 §2.4 的 142 维（39+ΔΔ²+64+std） | 训练复现口径混乱 | 统一 `features.py`，两种口径并存，84 维为主基线，扩展口径做消融 |
| 5 | **样本时长仅 1s**：可能截断整段咳嗽（正常类 1.027s 已超限） | 特征信息不完整 | ①按 1s 对齐（现状）②后续要求监测板块输出 ≥1.5s 片段 ③时序模型（CNN-BiLSTM）容忍段内多咳 |
| 6 | **第 6 病（猪肺疫）无音频数据** | 无标注类别的音频会被归入 5 类之一 | ①决策口径：恒出 5 类之一取最高置信度，不拒答（2026-08-14 定调）②论治层辨病纠错兜底（confidence 供其参考）③未来扩充类别 |
| 7 | **Python 3.14 兼容性**：librosa/numba 是否可用未验证（`test（测试草稿）.py` 在测） | 训练环境可能起不来 | ①首日先跑通依赖自测 ②不可用则降级 Python 3.11 虚拟环境 ③特征提取已入库，训练可脱离 librosa |
| 8 | **无评测基线落库**：`model_versions` / `training_datasets` 表空 | 版本追溯缺失 | 训练脚本自动写库（含特征参数哈希） |

---

## 四、技术路线（定稿）

### 4.1 数据工程

**① 标签审计（先行，1 天）**
- `unknown/` 2 条：人工听辨 → 归入某类或剔除出训练集（建议先剔除，后续作 OOD 测试）
- 各病类抽 3~5 条复听，核对标签与文件名来源（`pig_cough_data/` 目录即标签，音频文件名命名规则见 §2.1）
- 产出 `labels_audit.csv`：file_id, label, 复听意见, 是否入训练集

**② 统一特征管线 `features.py`（与库内口径完全一致，可复算）**

```python
# 主基线口径（与 pig_cough 库 84 维一致，直接可复现）
MFCC 13 (均值)  +  logFBank 64 (均值)  +  时域 3 (能量/ZCR/RMS)  +  频域 4 (质心/带宽/平坦度/滚降)  = 84 维
# 扩展口径（消融用，参考手册 §2.4 与文献）
MFCC 13+Δ+Δ² (均值+标准差) + logFBank 64 均值 = 142 维
```

- 参数：`sr=16000`（源 8k 重采样）、`n_fft=2048`、`hop=512`、`duration=1.0s` 截断/零填充
- 产出 `features_train.npz / features_val.npz / features_test.npz`（X, y, file_id 三件套，便于追溯样本）

**③ 数据增强（仅训练集，小样本核心手段）**

| 增强 | 参数 | 依据 |
|------|------|------|
| 时间拉伸 | rate ∈ [0.75, 1.25] | 纪楠(2023) |
| 音高偏移 | ±2 半音 | 王锡鹏(2024) |
| 加噪 | 高斯/粉噪 SNR 5~15dB | 纪楠(2023) |
| 时移 | ±0.2s | 付小朋(2025) |

- 病类样本 ≤50 条 → 每类增强至 ≥60 条（4~5 倍），normal 类不增强（防类间混淆）
- 增强样本**不入库**（防污染真实特征表），仅作训练时动态生成

**④ 数据划分（固定随机种子，保证复现）**

- 8:1:1 分层划分（`random_state=42`，按病类分层）
- 病类仅 13~14 条 → 验证/测试各 1~2 条/类，**5 折分层 CV 为主评估协议**，固定划分仅用于最终报告复现
- 划分清单落库 `training_datasets`（含文件清单 JSON）

### 4.2 模型路线（分阶段递进）

```
Step 0 快速体检 ──▶ Step 1 SVM Baseline ──▶ Step 2 SSLRB 主方案 ──▶ Step 3 CNN-BiLSTM(备选)
  特征有效性            先跑通              小样本+不平衡主力        时序建模能力
                                                    │
                                              Step 4 预训练迁移(可选)
                                              Wav2Vec2.0 / AST 微调
```

| 阶段 | 模型 | 说明 | 通过门槛（5折 Macro-F1） |
|------|------|------|--------------------------|
| Step 0 | LR / RF / SVM 快速体检 | 特征有效性验证，1 小时内出结果 | ≥0.60 继续；<0.60 先查数据问题 |
| Step 1 | SVM RBF（C=100, γ=0.01） | 手册 §3.1 代码就绪，验证集 ≥80% 为经验门槛 | ≥0.65 |
| **Step 2** | **SSLRB（主方案）** | 3 个 SVM 基分类器（γ∈{0.1,0.01,0.001}）3 折分层 CV 生成元特征 → 3 个 LR 元学习器 Bagging + ROS 上采样 | **≥0.72** |
| Step 3 | CNN-BiLSTM | 2 层 CNN + BiLSTM + Softmax，PyTorch；1s×8k 重采样 16k 的 logFBank 图输入 | ≥0.70 |
| Step 4 | Wav2Vec2.0 / AST 微调 | 小样本迁移，样本量 ≥300 时启动 | ≥0.75 |

- Step 0~2 仅依赖已入库 84 维特征（`extract_and_insert` 数据已备好），**不阻塞**；Step 3 起需 librosa 预处理管线
- 每个 Step 产出：模型权重 + 评估报告（5 折均值±std）+ `model_versions` 落库

### 4.3 评估体系

**主指标（不平衡数据下以 F1 为准，accuracy 仅作参考）**：

- Macro-F1（主判据）、各类 Precision/Recall/F1、混淆矩阵
- Accuracy、Weighted-F1、PR-AUC、G-mean（辅助）

**验收分级（小样本现实修正）**：

| 级别 | Macro-F1 | 病类平均召回 | 健康类召回 | 说明 |
|------|----------|-------------|-----------|------|
| 及格（可联调） | ≥0.65 | ≥0.60 | ≥0.90 | 论治层辨病纠错可兜底 |
| **达标（交付）** | **≥0.75** | **≥0.70** | **≥0.95** | 与技术路线"≥90%准确率"目标在小样本下的等价表述 |
| 挑战（数据扩容后） | ≥0.85 | ≥0.80 | ≥0.98 | 需病类样本 ≥100/类 + 迁移学习 |

> 说明：技术路线 §6.1 的"准确率 ≥90%"建立在 ≥500 条/类的大样本前提上；当前 13~14 条/类时 accuracy 会被 normal 类主导虚高，故改用 Macro-F1 为主判据。**数据扩容（监测板块持续采集 + 增强）是冲击 90% 准确率的必要路径**，列为长期任务。

### 4.4 工程落地（与决策部分代码风格一致）

```
辨病/
├── 辨病模块_实施路线文档.md        # 本文
├── features.py                    # 统一特征管线（与库内口径可复算）
├── labels_audit.csv               # 标签审计表
├── train_baseline.py              # Step 1 SVM
├── train_sslr.py                  # Step 2 主方案
├── train_cnn_bilstm.py            # Step 3（备选）
├── predict.py                     # 推理：wav → {disease, confidence, cough_type, symptoms}
├── bridge.py                      # 辨病→论治桥接（disease 别名归一 + 契约组装）
├── test_辨病.py                    # 测试套件（镜像决策部分 test_manual.py 风格）
└── pig_cough_data/                # 标注数据（现有）
```

**推理链路**：

```
wav ──▶ features.py 提取 84 维 ──▶ predict.py 分类 ──▶ bridge.py ──▶ 论治引擎 /api/diagnosis
                                                                    │
                                        disease 中文名 + confidence + symptoms + temp_c
```

- 决策口径（2026-08-14 定调）：**恒输出 5 类之一（健康 + 4 病），取置信度最高者，不拒答**；confidence 输出给论治层辨病纠错参考；猪肺疫/混合感染等未来扩充类别后纳入
- `bridge.py` 自动生成 `symptoms`（由 `cough_type` + 概率 Top2 疾病的典型症状模板组装），体温/精神字段留空待多模态接入
- 回写：推理结果写 `pig_diag_v2.diagnosis_record`（业务库），模型版本写 `model_version`（v2 单数表名）

**测试体系**（镜像决策部分，40 项测试风格）：

| 块 | 内容 |
|----|------|
| 特征复算 | 库中特征 vs `features.py` 复算误差 < 1e-6（随机抽 10 条） |
| 模型加载 | 权重加载、类别数 =5、预测形状正确 |
| 分类正确性 | 已知 5 类各抽 3 条，Top1 命中率 |
| 别名归一 | 猪蓝耳病≡猪繁殖与呼吸综合征 等映射通过 |
| 桥接契约 | bridge 输出 JSON 各字段齐全，论治引擎端到端 5 案例可跑通 |
| 恒输出 5 类 | 任意输入（含高斯噪声）→ 5 类之一，Top1 恒非空 |

### 4.5 里程碑（10 天计划）

```
Day 0-1  ── 数据审计：unknown 处置、抽审、标签审计表；环境自测（numba/librosa 兼容）
Day 2    ── features.py 统一管线 + 复算校验（对库内 198 行 × 4 列误差归零）；数据划分落库
Day 3    ── Step 0 快速体检 + Step 1 SVM Baseline（5 折报告）
Day 4-5  ── Step 2 SSLRB 主方案训练 + 数据增强管线 + 调参（γ/C/Bagging 数）
Day 6    ── 模型评估报告（混淆矩阵分析类间混淆：支原体 vs 流感 vs 胸膜肺炎）
Day 7    ── predict.py + bridge.py + 论治引擎端到端联调（含辨病纠错场景复测）
Day 8    ── test_辨病.py 测试套件 + model_versions 落库
Day 9    ── Step 3 CNN-BiLSTM（时间允许）/ 特征扩展消融实验
Day 10   ── 文档收尾：评估报告 + 本文档执行情况回填；演示链路验证
```

### 4.6 数据扩容路径（长期，冲击 90% 目标）

1. **监测板块回接**：猪场持续采集咳嗽片段（要求 ≥1.5s、8k/16k、带时间戳与圈舍号）
2. **主动学习**：模型低置信度样本 → 兽医听辨标注（金标准确诊 5%~20%）
3. **多模态接入**：体温/精神数据入 `multimodal_features` → 方案 C 跨模态融合

---

## 五、任务分工

| 任务 | 负责人 | 产出物 |
|------|--------|--------|
| 数据审计、unknown 处置、抽审复听 | 柴堰颖 | labels_audit.csv |
| 特征管线统一 + 复算校验 | 刘宇翔 | features.py + 校验报告 |
| Step 0/1/2 模型训练与评估 | 阴学舟、刘宇翔 | 模型权重 + 评估报告 |
| predict/bridge + 论治联调 | 阴学舟 | predict.py / bridge.py / 联调记录 |
| 测试套件 | 刘宇翔 | test_辨病.py |
| 文档（本文执行回填 + 评估报告） | 全员 | 实施文档 |

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 病类仅 13~14 样本 → 评测方差大、结论不可信 | 高 | 达标判定失真 | ①5 折分层 CV 报告均值±std ②增强后复测 ③结论注明样本量置信区间 |
| 正常类与咳嗽病类声学差异大 → 模型偏向"正常" | 高 | 病类召回低 | ①ROS 上采样 + class_weight ②Macro-F1 主判据 ③健康类高阈值独立评估 |
| 病类间声学混淆（支原体/流感/胸膜肺炎咳声相似） | 高 | 混淆矩阵集中错误 | ①混淆矩阵专项分析 ②logFBank 图 + CNN 时频细节 ③论治层辨病纠错兜底 |
| 标签噪声（目录名分类错误/双病混合） | 中 | 模型学习错误模式 | ①抽审复听 ②不一致样本剔除 ③unknown 不入选 |
| 1s 片段截断咳嗽 | 中 | 特征不完整 | ①与监测板块约定 ≥1.5s 片段 ②增强中含时移 ③后续升级时序模型 |
| librosa/numba 在 Python 3.14 不可用 | 中 | 训练阻塞 | ①Day 0 自测（草稿脚本已有）②不可用则 Python 3.11 venv ③训练可仅用已入库特征 |
| 第 6 病（猪肺疫）样本缺失 | 中 | 肺疫音频被归入近邻病类 | ①决策口径恒出 5 类（不拒答，2026-08-14 定调）②confidence 供论治层辨病纠错提示"多病可能"③未来补数据扩充类别 |
| 百炼 API 限流/延迟影响联调 | 低 | 端到端测试慢 | 沿用决策部分 Redis 缓存方案（待实现） |

---

## 七、文献依据（与路线报告 §3.1 对齐）

1. **纪楠(2023)**《基于多域特征融合的生猪咳嗽声识别方法研究》：A-TF 多域融合 + SSLRB 混合集成，重度不平衡 F1=92.19%；MFCC/logFBank/时域/频域特征集；时间拉伸/加噪/时移增强 — **主方案来源**
2. **王梦雨(2022)**《基于深度神经网络的猪只咳嗽声识别预警系统设计与实现》：MFCC-CNNs 四分类约 93%、logFBank CNN 二分类约 96% — Step 3 特征图输入参考
3. **付小朋(2025)**《基于CNN-BiLSTM的猪咳嗽声识别方法》：CNN 局部特征 + BiLSTM 时序建模 — Step 3 架构来源
4. **谢杰(2023)**《基于智能语音技术的猪只声音监测研究》：SVM(MFCC) ~87%、LSTM ~90% — Step 0/1 基线预期
5. **王锡鹏(2024)**《复杂环境下基于多分类器融合的猪咳嗽声自动检测方法研究》：多分类器融合 + 语谱图 LBP/HOG 特征 — 增强与特征扩展参考
6. 纪楠论文数据集规模（样本上万条）远超本项目 198 条 — 本路线 §4.6 数据扩容路径的立项依据

---

## 八、交付物清单

| # | 交付物 | 形式 | 验收 |
|---|--------|------|------|
| 1 | 标签审计表 | labels_audit.csv | 198 条意见可追溯 |
| 2 | 统一特征管线 | features.py | 与库内 198 行特征复算误差 <1e-6 |
| 3 | 辨病模型（Step 1→2 至少交付 SSLRB） | .pkl/.joblib + 权重说明 | 5 折 Macro-F1 ≥0.75（达标线） |
| 4 | 推理模块 | predict.py | 单条 wav 推理 <1s（含特征提取） |
| 5 | 辨病→论治桥接 | bridge.py | 端到端 5 案例跑通 + 别名归一测试 |
| 6 | 测试套件 | test_辨病.py | 6 块测试全绿 |
| 7 | 评估报告 | 评估报告.md | 5 折均值±std + 混淆矩阵分析 |
| 8 | 数据扩容计划 | 本文 §4.6 执行回填 | 与监测板块接口约定确认 |

---

## 九、关键决策记录（本模块）

| # | 决策 | 原因 |
|---|------|------|
| 1 | 主评估指标从 Accuracy 改为 **Macro-F1** | normal 占 54%，accuracy 被主导虚高；病类召回才是临床价值 |
| 2 | 主方案沿用 **SSLRB**，SVM 为基线、CNN-BiLSTM 为备选 | 与决策部分技术路线一致；小样本下传统 ML 更稳，深度学习需扩样后发力 |
| 3 | **84 维均值池化特征为主基线** | 与已入库 198 行完全一致，训练零阻塞、可复现；扩展 142 维仅作消融 |
| 4 | unknown 2 条**剔除出训练集** | 标签不可信，宁可少样本不要噪声 |
| 5 | 增强样本**不入库**，仅训练时动态生成 | 防污染真实特征表（acoustic_features 是后续多模态对齐的基准） |
| 6 | 5 折分层 CV 为主协议，固定 8:1:1 仅复现用 | 病类 13 条时单次划分方差太大 |
| 7 | **恒出 5 类不拒答** | 决策口径（2026-08-14）：辨病输出仅健康+4 病，取最高置信度；猪肺疫等类别未来扩充；confidence 供论治层纠错参考 |
| 8 | 单库 `pig_diag_v2` 统一 | 整合迁移已完成（26 表），训练数据与知识/业务同库，`.env` 指向 v2 |

---

## 十、执行进度（2026-08-14）

| 里程碑 | 状态 | 说明 |
|--------|------|------|
| Day 2 特征管线 | ✅ | `features.py` 落地（提取/复算校验/划分三合一）；与库内 198 行复算误差 < 1e-13（初测 1e-5 为 librosa 版本漂移，已用当前管线刷新库内特征实现误差归零） |
| Day 2 数据划分 | ✅ | 8:1:1 分层（v1: train 158/val 20/test 20；**v2 扩充集: train 239/val 30/test 30**，random_state=42），`features/*.npz`；`training_dataset` 落库（v2 行，record_ids 为真实 diagnosis id） |
| 特征存储重构 | ✅ | `acoustic_feature` 由 EAV 四行改宽表一行（mfcc/logfbank/temporal/spectral 四列），`diagnosis_record` 增 `file_source` 列回填 198 条；dump 全量同步并重导验证 |
| Day 0-1 数据审计 | ⏳ | unknown/ 2 条处置、标签抽审复听（labels_audit.csv）待做 |
| Day 3-5 模型 | ✅ | Step 0 体检 + Step 1 SVM Baseline + Step 2 SSLRB 全跑通；**2026-08-15 用扩充数据集（299 条）重训：SSLRB test Macro-F1 0.7933 达标**，评估/落库/推理闭环见下 |
| 推理模块 | ✅ | `predict.py`：wav → 诊断契约（label/disease/confidence/symptoms/top3），**恒出 5 类之一不拒答**（2026-08-14 决策口径；unknown/51.wav 实测 → prrs 0.31） |

### 模型评估结果（2026-08-17，六类数据集 540 条，test 54 条独立评估）

| 模型 | 5 折 CV Macro-F1 | Test Acc | Test Macro-F1 | 健康类召回 | 结论 |
|------|-----------------|----------|---------------|-----------|------|
| LR (84维) | 0.663 ± 0.082 | — | — | — | 快速体检 |
| RF (84维) | 0.706 ± 0.086 | — | — | — | 快速体检 |
| SVM Baseline (84维) | 0.761 ± 0.043 | 0.833 | 0.7300 | 0.941 | 接近达标 |
| **SSLRB (84维)** | 0.725 ± 0.042 | 0.833 | **0.7516** | 0.941 | **当前最优，过达标线** |

**全样本回测（in-sample，含训练数据）**：540 条准确率 **95.37%**（515/540）；other_disease 13/13 全对；误判 25 条（报告：`全样本识别v3报告.md` / `全样本识别v3结果.csv`）。

**结论**：六类（含 other_disease）下 SSLRB test Macro-F1 **0.7516** 过达标线（≥0.75）。6 类任务比 5 类更难（v2 五类 0.7933），健康召回 0.941 略低于 0.95 线——继续扩充 prrs/app 样本可补足。

| 模块联调 | ✅ | 三层解耦架构落地：辨病独立（predict.py）/ 论治独立（therapy_engine）/ 桥接（bridge.py）/ 编排（pipeline.py + api_server `/api/diagnose-audio`），全链路实测通过（见 §11 调用方式） |

**下一步**：① Day 0-1 数据审计收尾；② 数据扩容路径启动（与监测板块约定片段规格）。注意：仅针对 5 类有标注疾病（normal/influenza/prrs/mycoplasma/app），猪肺疫/混合感染等其余类别在未来开发中完善。

---

## 十一、调用方式（辨病 / 论治 解耦与串联）

**分层约定**（互不 import，各自可独立部署/测试）：

```
辨病模块（辨病/）          论治模块（根目录）
  predict.py                 therapy_engine.py
  wav → 诊断 JSON           诊断+症状 → 方案 JSON
        │                        ▲
        └─── bridge.py ──────────┘
            契约转换（唯一耦合点）
        └─── pipeline.py / api_server ─── 编排层（串联）
```

| 场景 | 调用方式 | 说明 |
|------|---------|------|
| 只辨病 | `python 辨病/predict.py x.wav` | 诊断 JSON（label/disease/confidence/top3） |
| 只辨病（交互） | `python 辨病/interactive_audio_demo.py` | 选样本/路径 + 概率分布 |
| 只论治 | `python interactive_demo.py` 或 `POST /api/diagnosis` | 辨证+组方+校验（输入疾病+症状） |
| 完整流程（CLI） | `python pipeline.py x.wav --temp 39.2 --weight 70 --severity 中度` | 辨病→桥接→论治 全链路报告 |
| 完整流程（API） | `POST /api/diagnose-audio`（multipart wav + 可选参数） | 全链路 + 落库（diagnosis_record + prescription_record，trace_id 返回） |
| Python 嵌入 | `from pipeline import full_pipeline` | 返回 {diagnosis, meta, therapy, result} |

**契约要点**：
- 辨病输出：`{label, disease, confidence, typical_symptoms, top3}`（label 与 DB disease.label 一致；**typical_symptoms 仅展示**）
- 桥接（`辨病/bridge.py`）：label→中文病名、temp/weight/severity 默认参数；**论治 symptoms 只来自手动输入**（缺省空串，不自动用典型症状兜底）；是两端唯一契约转换点
- 论治输入：`treatment_pipeline(disease, symptoms, temp, weight, severity, pig_extra)` —— symptoms 为实际症状
- 配置：辨病用 `bianbing_config.py`（读根 .env），论治用根 `config.py` —— 模块名唯一，避免 sys.path 碰撞

---

*本文档随实施推进回填执行结果；里程碑完成情况同步至《诊疗决策板块_开发日志.md》。*
