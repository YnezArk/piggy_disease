# 诊疗决策板块 — 数据库设计 v2.0（整合版）

> **版本**：v2.0（整合 pig_cough + pig_diag）
> **日期**：2026-08-14
> **数据库**：`pig_diag_v2`（MySQL 5.7+，原 pig_diag 弃用）
> **建库脚本**：`pig_diag_v2.sql`（Navicat 完整导出，26 表全量可重建）
> **字符集**：utf8mb4
> **关联文档**：《猪咳嗽项目数据库说明文档（诊疗版）.md》（旧口径，已过时）、《过时_诊疗决策板块_数据库设计.md》（v1 设计基线）

---

## 一、版本变更说明（v1 → v2）

### 1.1 变更概述

原 `pig_cough`（辨病训练数据）与 `pig_diag`（论治知识库）已**整合迁移**为 `pig_diag_v2` 单库，共 **26 张表**。整合后 `pig_diag` 弃用，一切连接指向 `pig_diag_v2`。

```
pig_cough（5.7，30+表） ──┐
                         ├──▶ pig_diag_v2（26 表）◀── 当前唯一事实源
pig_diag（8.4，12表）   ──┘
```

### 1.2 变更清单

| 变更项 | v1 | v2 | 说明 |
|--------|----|----|------|
| 数据库名 | pig_cough / pig_diag 并存 | **pig_diag_v2 单库** | 代码连接配置需改 |
| 知识底座 9 表 | pig_diag 12 表 | 保留，字段不变 | syndrome_mapping 增 `weight` 权重列 |
| 业务记录 3 表 | pig_diag | 保留 | diagnosis_record 增 `model_label` 输出标签列 |
| 声学/多模态特征 | pig_cough | 并入（acoustic_feature / multimodal_feature） | **模型训练核心数据**，198 条已迁移 |
| 养殖管理 | pig_cough | 并入（pig_farms/pig_groups/pigs/users） | 新增 user_group 关联表 |
| 农户指导 | pig_cough | 并入（farmer_guidance/guidance_execution） | 新增 urgency_level/generated_by 字段 |
| 系统支撑 | pig_cough | 并入（model_version/training_dataset/agent_log/system_config/dashboard_stats） | 新增 dashboard_stats 看板 |
| 知识来源 | — | kg_source 13 条 | 防幻觉引用 |

### 1.3 对模型组的影响

| 影响项 | 说明 |
|--------|------|
| 训练数据 | `diagnosis_record` JOIN `acoustic_feature`（198 样本，每样本一行特征），SQL 模板见 §9 |
| 连接配置 | `.env` / `config.py` 中 `DB_NAME=pig_diag_v2`（原 pig_cough 保留为备份） |
| 表/字段 | 训练相关表名、字段名与旧 pig_cough 口径不同（`diagnosis_record`/`acoustic_feature` 单数命名），代码按本文档 §9 模板调整 |
| 标签口径 | `disease.label` = 标注文件夹名 = 模型输出标签，三者统一（见 §11） |

---

## 二、总体设计

### 2.1 存储分工

| 存储 | 用途 | 说明 |
|------|------|------|
| **MySQL（pig_diag_v2）** | 结构化知识（辨证/论治/药材/禁忌）+ 业务记录 + 声学特征 | **事实权威源**，支持 Text-to-SQL 精确查询 |
| **内存 RAG 索引**（`rag_retrieve.py`） | 典籍语料 + 双库向量化副本 | 自研 jieba 哈希 + 余弦检索（ChromaDB 已弃用：hnswlib 在 Py3.14 卡死，见开发日志决策#1），命中后回 MySQL 取完整记录 |
| **JSON/CSV** | 离线种子数据、导出备份 | 与 MySQL 可互导 |

> 关系：**MySQL 是权威源**，RAG 索引是 MySQL 的投影，重建成本极低（重启进程自动重建）。

### 2.2 ER 关系总览（理-法-方-药逻辑链 + 业务闭环）

```
┌──────────────┐    判定     ┌────────────┐   适用    ┌────────────┐
│ syndrome_    │───────────▶│ syndrome   │─────────▶ │ formula    │
│ mapping      │ 体征组合→证候 │ (证候/治则) │ 证候→方剂  │ (方剂)     │
│ (辨证库)     │             └────────────┘           └─────┬──────┘
└──────────────┘                                           │ 组成
                                                           ▼
                                  ┌────────────┐    关联    ┌────────────┐
                                  │ formula_   │──────────▶ │ herb       │
                                  │ herb       │ 方剂-药材   │ (药材)     │
                                  └────────────┘           └─────┬──────┘
                                                                 │ 禁忌
                                                                 ▼
                                      ┌───────────────────────────────┐
                                      │ herb_contraindication         │
                                      │ (十八反十九畏/孕猪禁用)        │
                                      └───────────────────────────────┘

业务链（闭环反馈）：
  diagnosis_record ──▶ prescription_record ──▶ treatment_feedback ──▶ farmer_guidance
  (辨病结果+声学特征)     (组方+校验)             (疗效追踪)              (农户指导)

养殖链（上下文）：
  pig_farms ──▶ pig_groups ──▶ pigs          users ──▶ user_group ──▶ pig_groups
```

### 2.3 命名规范

- 表名：小写蛇形单数（`acoustic_feature`），部分继承 pig_cough 复数（`pig_farms`）保留不改
- 字段：小写蛇形（`cough_type`）
- 主键：`id` / `xxx_id`，`BIGINT UNSIGNED AUTO_INCREMENT`
- 外键：`xxx_id`；时间：`created_at` / `created_time` 两套并存（继承各自源头，不强制统一）
- 字符集：`utf8mb4`；知识表含 `reference_id`（引用来源，杜绝幻觉出处）
- 枚举：字段级 `ENUM`（model_version.model_type、guidance_type 等）

---

## 三、知识底座（9 表，论治层数据底座）

> 沿用 v1 设计（§2），仅列 v2 实际字段。理-法-方-药逻辑链落库路径：
> `syndrome_mapping(多维体征) → syndrome(证候=理+法) → formula(方) → formula_herb(药+剂量) └ add_rule(随症加减) → herb(药物属性)`

### 3.1 `kg_source` 知识来源表（防幻觉引用）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| title | VARCHAR(256) NOT NULL | 文献/出处标题 |
| category | VARCHAR(32) | 教材/典籍/临床病例/PPT验证 |
| detail | VARCHAR(512) | 章节/页码定位 |
| created_at | DATETIME | 创建时间 |

**当前数据**：13 条。

### 3.2 `disease` 疾病表（辨病标签 + 别名归一）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| name | VARCHAR(64) NOT NULL UNIQUE | 疾病名 |
| alias | VARCHAR(256) | 别名逗号分隔（俗名/缩写），如：蓝耳病,PRRS |
| pathogen | VARCHAR(128) | 病原体 |
| epidemiology | VARCHAR(512) | 流行病学特点 |
| symptoms | VARCHAR(512) | 典型症状 |
| **label** | VARCHAR(64) | **辨病模型分类标签（英文小写）** |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

**当前数据**（7 条，label 与辨病标注文件夹一致）：

| id | name | label | alias |
|----|------|-------|-------|
| 1 | 健康猪只 | `normal` | 健康,正常 |
| 2 | 猪流行性感冒 | `influenza` | 猪流感,SI,Swine Influenza |
| 3 | 猪蓝耳病 | `prrs` | 蓝耳病,猪蓝耳病,PRRS,猪繁殖与呼吸障碍综合征,神秘猪病,HP-PRRS |
| 4 | 猪支原体肺炎 | `mycoplasma` | 猪气喘病,支原体肺炎,Mhp |
| 5 | 猪传染性胸膜肺炎 | `app` | APP,胸膜肺炎放线杆菌病 |
| 7 | 猪肺疫 | `pasteurella` | 猪巴氏杆菌病,锁喉风,肿脖子瘟,Pasteurella multocida |
| 8 | 混合感染 | `mixed` | PRDC,多病原混合感染 |

> 说明：蓝耳病一行（id3）别名收录 PRRS 全称，别名归一可双向匹配（蓝耳病≡猪繁殖与呼吸综合征）；id7 猪肺疫、id8 混合感染暂无音频标注数据，辨病模型不输出（OOD 处理见 §11）。

### 3.3 `syndrome` 证候表（治则治法挂此表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| name | VARCHAR(64) NOT NULL UNIQUE | 证候名，如：疫热壅肺 |
| stage | VARCHAR(32) NOT NULL | 中医分期：前期/中期/后期 |
| description | VARCHAR(512) | 证候描述 |
| principle | VARCHAR(128) NOT NULL | 治则治法 |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

**当前数据**：8 条（邪袭肺卫/疫热壅肺/气阴两伤/风热犯肺/肺寒咳嗽/肺气虚咳嗽/痰饮阻肺/肺热咳喘）。

### 3.4 `syndrome_mapping` 辨证库（多维体征 → 证候）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| temperature_min/max | DECIMAL(4,1) | 体温区间，NULL=不限 |
| cough_type | VARCHAR(128) | 咳喘表现关键词（湿咳、痰多、干咳…） |
| excretion | VARCHAR(128) | 排泄物观察 |
| other_signs | VARCHAR(256) | 其他体征：精神、采食、呼吸等 |
| syndrome_id | BIGINT UNSIGNED NOT NULL | 命中证候 |
| disease_id | BIGINT UNSIGNED | 关联疾病（可空） |
| evidence | VARCHAR(512) | 鉴别依据（专家标注） |
| **weight** | TINYINT UNSIGNED DEFAULT 5 | **权重 1-10**（v2 新增，同体征多规则优先） |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

**当前数据**：11 条规则（蓝耳病急性/慢性 2 条关联 disease_id 3；规则 2/3 关联混合感染 id8、规则 4 关联猪肺疫 id7）。

### 3.5 `formula` 方剂表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| name | VARCHAR(64) NOT NULL UNIQUE | 方剂名 |
| syndrome_id | BIGINT UNSIGNED NOT NULL | 主治证候 |
| usage_method | VARCHAR(256) | 用法（按500g饲料混饲…） |
| course | VARCHAR(64) | 疗程（连用5-7天…） |
| notes | VARCHAR(512) | 方解/备注 |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

**当前数据**：20 条。

### 3.6 `formula_herb` 方剂组成表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| formula_id | BIGINT UNSIGNED NOT NULL | 方剂 |
| herb_id | BIGINT UNSIGNED NOT NULL | 药材 |
| dosage_g | DECIMAL(6,1) NOT NULL | 基础剂量（克） |
| role | VARCHAR(16) NOT NULL | 君臣佐使 |
| note | VARCHAR(256) | 该药在方中作用说明 |
| sort_order | TINYINT UNSIGNED DEFAULT 0 | 排列顺序 |
| created_at | DATETIME | 创建时间 |

UNIQUE(formula_id, herb_id)。**当前数据**：126 条（20 方组成明细）。

### 3.7 `add_rule` 随症加减规则表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| formula_id | BIGINT UNSIGNED NOT NULL | 所属方剂 |
| condition_desc | VARCHAR(256) NOT NULL | 加减条件（痰多加…） |
| herb_id | BIGINT UNSIGNED | 加减药物（可空=减药） |
| operation | VARCHAR(8) DEFAULT '加' | 加/减/调整剂量 |
| dosage_g | DECIMAL(6,1) | 加减剂量 |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

**当前数据**：11 条。

### 3.8 `herb` 药材表（安全剂量区间）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| name | VARCHAR(64) NOT NULL UNIQUE | 药材名 |
| property | VARCHAR(16) | 四性（寒热温凉平） |
| flavor | VARCHAR(32) | 五味（辛甘酸苦咸） |
| meridian | VARCHAR(64) | 归经（肺/脾/胃…） |
| effect | VARCHAR(256) | 功效 |
| dosage_min/max | DECIMAL(6,1) | 安全剂量区间（克） |
| caution | VARCHAR(256) | 使用注意（孕猪禁用等） |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

**当前数据**：79 条。

### 3.9 `herb_contraindication` 配伍禁忌表（安全护栏）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| herb_a / herb_b | BIGINT UNSIGNED NOT NULL | 药材对 |
| rule_type | VARCHAR(16) NOT NULL | 十八反/十九畏/孕猪禁用/其他 |
| description | VARCHAR(256) | 说明 |
| reference_id | BIGINT UNSIGNED | 知识来源 |
| created_at | DATETIME | 创建时间 |

UNIQUE(herb_a, herb_b)。**当前数据**：8 条。

---

## 四、业务记录（3 表，闭环反馈）

### 4.1 `diagnosis_record` 辨病记录（⭐ 模型输出落表 + 训练样本主表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键（有效样本从 371 起） |
| file_source | VARCHAR(128) | **来源音频文件**（辨病/pig_cough_data/ 相对路径，如 normal/1.wav） |
| pig_house | VARCHAR(64) | 猪舍编号 |
| **model_label** | VARCHAR(64) | **辨病模型实际输出标签**（v2 新增） |
| disease_id | BIGINT UNSIGNED | 命中疾病（外键→disease.id） |
| confidence | DECIMAL(4,3) | 置信度 0-1 |
| temp_c | DECIMAL(4,1) | 体温℃ |
| mental_state | VARCHAR(64) | 精神状态 |
| env_json | JSON | 环境参数快照（氨气/温湿度） |
| feature_json | JSON | 声学特征快照（可选） |
| created_at | DATETIME | 创建时间 |

**当前数据**：198 条（371-568）。disease_id 分布与标注文件夹一致：**1=107（健康）/ 2=50（流感）/ 3=14（蓝耳）/ 4=14（支原体）/ 5=13（胸膜肺炎）** —— 辨病训练标签依据（映射见 §11.1）。

### 4.2 `prescription_record` 处方记录（组方 + 校验结果）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| diagnosis_id | BIGINT UNSIGNED | 关联辨病记录 |
| syndrome_id | BIGINT UNSIGNED | 辨证结果-证候 |
| formula_id | BIGINT UNSIGNED | 基础方剂 |
| herbs_json | JSON | 组方明细 [{name, dosage_g, role}] |
| usage_method | VARCHAR(256) | 用法 |
| course | VARCHAR(64) | 疗程 |
| safety_approved | TINYINT(1) DEFAULT 0 | 校验是否通过 |
| safety_report | JSON | 校验报告 {warnings, issues, score} |
| llm_raw | TEXT | LLM 原始输出（审计用） |
| references_json | JSON | 引用出处列表 |
| created_at | DATETIME | 创建时间 |

**当前数据**：空（组方 Agent 运行时写入）。

### 4.3 `treatment_feedback` 疗效反馈表（闭环）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT UNSIGNED PK | 主键 |
| prescription_id | BIGINT UNSIGNED NOT NULL | 关联处方 |
| day_after | TINYINT UNSIGNED | 用药第 N 天 |
| cough_freq_change | VARCHAR(32) | 咳嗽频率变化（-40%） |
| temp_change | VARCHAR(32) | 体温变化（-0.8℃） |
| mental_change | VARCHAR(64) | 精神状态变化 |
| outcome | VARCHAR(32) | 治愈/好转/无效/恶化 |
| adjustment | VARCHAR(256) | 方案调整 |
| effective_score | DECIMAL(3,2) | 疗效评分 0-1 |
| created_at | DATETIME | 创建时间 |

**当前数据**：空（疗效追踪时写入）。

---

## 五、声学特征与多模态（2 表，⭐ 模型输入）

### 5.1 `acoustic_feature` 声学特征表（模型输入 X）

**宽表设计：一个样本一行，四类特征并列存储**（2026-08-14 由旧 EAV 四行重构，一行即一个样本的全部输入特征）。

| 字段 | 类型 | 说明 |
|------|------|------|
| feature_id | INT PK AUTO_INCREMENT | 主键 |
| diagnosis_id | BIGINT UNSIGNED NOT NULL | 关联 diagnosis_record.id |
| mfcc | JSON | MFCC 13 维（均值池化） |
| logfbank | JSON | logFBank 64 维（均值池化） |
| temporal | JSON | 时域特征 3 维（能量/过零率/RMS） |
| spectral | JSON | 频域特征 4 维（质心/带宽/平坦度/滚降） |
| feature_dimension | INT DEFAULT 84 | 总维度 13+64+3+4 |
| extracted_time | DATETIME DEFAULT NOW() | 提取时间 |

**拼接即 84 维输入向量。当前数据：198 行（每样本一行，与 diagnosis_record 一一对应）。**

### 5.2 `multimodal_feature` 多模态特征表（体温/行为/环境）

| 字段 | 类型 | 说明 |
|------|------|------|
| feature_id | INT PK AUTO_INCREMENT | 主键 |
| diagnosis_id | BIGINT UNSIGNED NOT NULL | 关联 diagnosis_record.id |
| feature_source | VARCHAR(30) | 体温监测/行为识别/环境传感器 |
| feature_data | JSON NOT NULL | 特征数据 |
| collected_time | DATETIME DEFAULT NOW() | 采集时间 |

**当前数据**：空（监测板块多模态接入后写入；辨病路线方案 C 的数据位）。

---

## 六、养殖管理（4 表）+ 用户关联（1 表）

### 6.1 `pig_farms` 养殖场

| 字段 | 类型 | 说明 |
|------|------|------|
| farm_id | INT PK AUTO_INCREMENT | 养殖场 ID |
| farm_name | VARCHAR(100) NOT NULL | 名称 |
| location / scale / contact_person / contact_phone | VARCHAR | 地址/规模/联系人/电话 |

**当前数据**：1 条（测试）。

### 6.2 `pig_groups` 猪群

| 字段 | 类型 | 说明 |
|------|------|------|
| group_id | INT PK AUTO_INCREMENT | 猪群 ID |
| farm_id | INT NOT NULL | 所属养殖场 |
| group_name / breed / age_range / quantity / house_conditions | VARCHAR/INT | 名称/品种/日龄/存栏/圈舍条件 |

**当前数据**：3 条（测试）。

### 6.3 `pigs` 个体猪只

| 字段 | 类型 | 说明 |
|------|------|------|
| pig_id | INT PK AUTO_INCREMENT | 猪只 ID |
| group_id | INT NOT NULL | 所属猪群 |
| ear_tag | VARCHAR(50) | 耳标号 |
| birth_date | DATE | 出生日期 |
| weight_kg | DECIMAL(8,2) | 体重（剂量换算输入） |
| gender | ENUM('公','母','阉') DEFAULT '阉' | 性别 |

**当前数据**：空。

### 6.4 `users` 用户 + `user_group` 用户-猪群关联

`users`：user_id / username(UNIQUE) / farm_id(外键) / created_time
`user_group`：id / user_id / group_id，UNIQUE(user_id, group_id)，双外键级联删除

**当前数据**：users 1 条，user_group 空。

---

## 七、农户指导（2 表）

### 7.1 `farmer_guidance` 指导建议

| 字段 | 类型 | 说明 |
|------|------|------|
| guidance_id | INT PK AUTO_INCREMENT | 指导 ID |
| group_id | INT NOT NULL | 猪群 |
| feedback_id | INT | 基于的反馈（关联 treatment_feedback） |
| prescription_id | BIGINT UNSIGNED | 关联处方 |
| guidance_time | DATETIME DEFAULT NOW() | 生成时间 |
| guidance_type | ENUM(初诊建议/增减用量/换方/停药/加强护理/紧急处理) | 指导类型 |
| content | TEXT NOT NULL | 指导正文 |
| suggested_adjustments | JSON | 建议调整 [{herb_name, change, reason}] |
| urgency_level | ENUM(普通/紧急) DEFAULT 普通 | 紧急程度 |
| is_executed | TINYINT(1) DEFAULT 0 | 是否已执行 |
| executed_time / executed_result | DATETIME / TEXT | 执行时间/结果 |
| generated_by | ENUM(AI/兽医/系统) DEFAULT AI | 生成方 |
| agent_version | VARCHAR(30) | 生成 Agent 版本 |

**当前数据**：空。

### 7.2 `guidance_execution` 执行反馈

execution_id / guidance_id / farmer_response / implementation_status ENUM(已采纳/部分采纳/未采纳/待确认) / implementation_result / feedback_time

**当前数据**：空。

---

## 八、系统支撑（5 表）

### 8.1 `model_version` 模型版本管理

model_id / model_name / version / model_type ENUM(诊断/辨证/组方/安全审核) / description / training_dataset_desc / hyperparameters JSON / performance_metrics JSON / model_path / deployed_time / is_active

**用途**：辨病模型每次训练落库（含超参数 + 5 折指标），`is_active=1` 为当前生效版本。当前空。

### 8.2 `training_dataset` 训练数据集管理

dataset_id / dataset_name / dataset_type ENUM(诊断/辨证/组方/综合) / description / record_ids JSON / total_samples / label_distribution JSON / created_time / created_by

**用途**：辨病数据划分（8:1:1 训练/验证/测试）落库，含 record_ids 与标签分布。当前空。

### 8.3 `agent_log` Agent 运行日志

log_id / run_id（一次运行关联多 Agent）/ agent_name ENUM(DxAgent/SxAgent/RxAgent/VxAgent) / diagnosis_id / input_data JSON / output_data JSON / prompt_text TEXT / tokens_used / response_time_ms / status ENUM(成功/失败/超时) / error_message / run_time

**用途**：论治引擎全链路审计。当前空。

### 8.4 `system_config` 系统配置

config_id / config_key(UNIQUE) / config_value / config_group / description / updated_time

**当前数据**（4 条）：`qwen_model=qwen-plus`（LLM）、`diagnosis_threshold=0.75`（辨病置信度阈值）、`max_herb_count=15`（单方最大药材数）、`rag_top_k=5`（RAG Top-K）。

### 8.5 `dashboard_stats` 统计看板

stat_id / stat_date / total_diagnosis / total_prescriptions / pending_guidance / recovery_rate / avg_confidence / created_time

**用途**：前端看板日统计。当前 1 条测试数据。

---

## 九、模型训练数据读取指南（辨病模块）

### 9.1 训练数据 SQL（特征 + 标签）

宽表结构下无需转置，直接联表即得完整训练集：

```sql
SELECT d.id AS diagnosis_id, d.disease_id, d.model_label, d.file_source,
       a.mfcc, a.logfbank, a.temporal, a.spectral
FROM diagnosis_record d
JOIN acoustic_feature a ON d.id = a.diagnosis_id
WHERE d.id >= 371;
```

> 注：`d.model_label` 为模型运行期输出（人工标注记录为空）；训练标签以 **disease_id** 为准，标签映射见 §3.2 / §11.1；`file_source` 记录来源音频（`辨病/pig_cough_data/` 相对路径）。

### 9.2 特征拼接（84 维）

```python
import pymysql, json, numpy as np

conn = pymysql.connect(host='localhost', user='root', password='***', database='pig_diag_v2', charset='utf8mb4')
cur = conn.cursor()
cur.execute("""SELECT d.id, d.disease_id, a.mfcc, a.logfbank, a.temporal, a.spectral
               FROM diagnosis_record d JOIN acoustic_feature a ON d.id = a.diagnosis_id
               WHERE d.id >= 371 ORDER BY d.id""")
X, y = [], []
for rid, disease_id, mfcc, logfbank, temporal, spectral in cur.fetchall():
    X.append(np.concatenate([
        np.array(json.loads(mfcc)), np.array(json.loads(logfbank)),
        np.array(json.loads(temporal)), np.array(json.loads(spectral))]))  # (84,)
    y.append(disease_id - 1)   # 0-based label，disease_id ∈ {1..5}

X = np.array(X)  # (198, 84)
y = np.array(y)  # (198,)
```

---

## 十、检索对接说明

| 场景 | 查询方式 | 说明 |
|------|---------|------|
| RAG 辨证检索 | `rag_retrieve.py` 内存索引 → 回表 `syndrome_mapping` 取完整规则 | 语义匹配体征描述（`FULL_KB=True` 小库全量注入，见技术路线） |
| Text-to-SQL 精确检索 | LLM 生成 SQL → MySQL | 条件明确（体温区间）命中 `syndrome_mapping`（含 weight 加权） |
| 组方检索 | `formula` JOIN `formula_herb` JOIN `herb` | 按证候取方剂+剂量+安全区间 |
| 安全校验 | `herb` 剂量区间 + `herb_contraindication` 查表 | 确定性规则，非 LLM（`safety.py`） |
| 别名归一 | `disease.alias` + `kb_lookup._same_disease()` | 辨病纠错（别名口径见 §11.2） |
| 疗效分析 | `treatment_feedback` 聚合 | 按处方/证候统计有效率 |
| 业务报表 | `diagnosis_record` + `prescription_record` + `dashboard_stats` | 前端小程序展示 |

---

## 十一、辨病标签与命名约定

### 11.1 标签统一口径

**`disease.label`（DB）= 标注文件夹名（`辨病/pig_cough_data/`）= 模型输出标签**，三者一致：

| 口径 | 值 |
|------|----|
| disease_id → label | {1: `normal`, 2: `influenza`, 3: `prrs`, 4: `mycoplasma`, 5: `app`} |
| 标注文件夹 | normal / influenza / mycoplasma / prrs / app（+ unknown 未分类桶） |
| label → 疾病名 | `DISEASE_LABEL_MAP`（见下） |

```python
DISEASE_LABEL_MAP = {
    'normal': '健康猪只', 'influenza': '猪流行性感冒', 'prrs': '猪蓝耳病',
    'mycoplasma': '猪支原体肺炎', 'app': '猪传染性胸膜肺炎',
    'pasteurella': '猪肺疫', 'mixed': '混合感染',      # OOD，模型不输出
}
```

- 训练标签以 `diagnosis_record.disease_id` 为准（198 条分布：1=107 / 2=50 / 3=14 / 4=14 / 5=13）
- `diagnosis_record.model_label` 为模型运行期实际输出值（人工标注记录为空）

### 11.2 别名归一约定

- 蓝耳病一行（id3）别名收录 PRRS 全称（猪繁殖与呼吸综合征/障碍综合征、HP-PRRS 等），`kb_lookup._same_disease()` 双向可匹配（"猪蓝耳病"≡"猪繁殖与呼吸综合征"）
- 猪肺疫（id7）别名含 锁喉风/肿脖子瘟/猪巴氏杆菌病/Pasteurella multocida，与蓝耳病语义完全隔离

### 11.3 决策口径（2026-08-14 定调）

- **辨病模型恒输出 5 类之一**（健康 + 4 病：normal/influenza/prrs/mycoplasma/app），取置信度最高者，**不做拒答**
- `pasteurella`（猪肺疫）、`mixed`（混合感染）无音频标注数据，暂不属于模型输出类别；未来补数据后扩充类别
- `confidence` 随结果输出，供论治层辨病纠错参考（不参与拒答判定）

---

## 十二、数据规模总览（v2 实际值）

| 表 | 数据量 | 说明 |
|----|--------|------|
| disease | 7 | 6 病 + 1 健康 |
| syndrome | 8 | 证型字典（三期 + 5 细分） |
| syndrome_mapping | 11 | 辨证规则 |
| herb | **79** | 药材库（诊疗版文档称 88，以 v2 实际为准） |
| formula | **20** | 方剂（诊疗版文档称 22，以 v2 实际为准） |
| formula_herb | **126** | 方剂-药材关联（诊疗版文档称 130，以 v2 实际为准） |
| add_rule | 11 | 加减规则 |
| herb_contraindication | 8 | 配伍禁忌 |
| kg_source | 13 | 知识来源 |
| diagnosis_record | 198 | 有效辨病记录（371-568） |
| acoustic_feature | 198 | 声学特征（每样本一行，84 维四列并列） |
| system_config | 4 | 系统配置 |
| pig_farms / pig_groups / users / dashboard_stats | 1 / 3 / 1 / 1 | 测试数据 |
| 其余 10 表（pigs/多模态/处方/反馈/指导/日志/模型版本/数据集/关联） | 0 | AI 运行时写入 |

> 注：诊疗版文档（v1 口径）中的表数 34、herb 88、formula 22、formula_herb 130 均为迁移前统计，v2 以本表为准。

---

## 十三、快速部署与验证

### 13.1 部署

```bash
CREATE DATABASE IF NOT EXISTS pig_diag_v2 DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
mysql -u root -p pig_diag_v2 < pig_diag_v2.sql   # 或 Navicat 运行 SQL 文件
```

### 13.2 连接配置

```
host: localhost  port: 3306  database: pig_diag_v2  charset: utf8mb4
```

代码侧：`.env` 的 `DB_NAME` 改为 `pig_diag_v2`；`config.py` 已集中读取，无需改代码逻辑。

### 13.3 验证清单

```sql
USE pig_diag_v2;
-- 1. 表数量 = 26
SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA='pig_diag_v2';
-- 2. 核心数据量
SELECT 'disease' t, COUNT(*) FROM disease UNION ALL
SELECT 'syndrome', COUNT(*) FROM syndrome UNION ALL
SELECT 'herb', COUNT(*) FROM herb UNION ALL
SELECT 'formula', COUNT(*) FROM formula UNION ALL
SELECT 'formula_herb', COUNT(*) FROM formula_herb UNION ALL
SELECT 'diagnosis_record', COUNT(*) FROM diagnosis_record UNION ALL
SELECT 'acoustic_feature', COUNT(*) FROM acoustic_feature;
-- 3. 样本分布（应 107/50/14/14/13）
SELECT disease_id, COUNT(*) FROM diagnosis_record WHERE id>=371 GROUP BY disease_id;
```

### 13.4 常见问题

- **Q1：id 为什么从 371 起？** 早期手动测试数据（1-370）无声学特征，已排除。
- **Q2：model_label 与 label 区别？** `disease.label` = 标准分类标签（模型训练用）；`diagnosis_record.model_label` = 模型实际输出值（运行期写入，当前 NULL）。
- **Q3：原 pig_cough 保留吗？** 保留作备份，v2 稳定后归档删除。
- **Q4：建库脚本为何用 v2 而非诊疗版文档的 v1？** v1 脚本已过时（pig_diag 库 + 旧数据量），`pig_diag_v2.sql` 为当前唯一权威全量导出。

---

## 十四、数据字典速查（26 表一览）

| 分组 | 表 | 职责 | 写入者 |
|------|----|------|--------|
| 知识底座 | kg_source | 知识出处（防幻觉） | 建库种子 |
| | disease | 疾病字典 + 别名 + 模型标签 | 建库种子 |
| | syndrome | 证候/治则 | 建库种子 |
| | syndrome_mapping | 多维体征→证候（辨证库） | 建库种子 |
| | herb | 药材属性 + 安全剂量 | 建库种子 |
| | formula | 方剂主数据 | 建库种子 |
| | formula_herb | 方剂组成 + 剂量 | 建库种子 |
| | add_rule | 随症加减规则 | 建库种子 |
| | herb_contraindication | 十八反十九畏（安全护栏） | 建库种子 |
| 业务记录 | diagnosis_record | 辨病输出（⭐ 样本主表） | 模型推理 / 人工标注 |
| | prescription_record | 组方 + 校验输出 | 组方 Agent |
| | treatment_feedback | 疗效追踪（闭环） | 系统 |
| 声学特征 | acoustic_feature | 声学特征向量（⭐ 模型输入） | 特征提取脚本 |
| | multimodal_feature | 体温/行为/环境特征 | 监测板块 |
| 养殖管理 | pig_farms / pig_groups / pigs / users | 养殖上下文 + 用户 | 运营 |
| 用户关联 | user_group | 用户-猪群权限 | 运营 |
| 农户指导 | farmer_guidance / guidance_execution | 指导建议 + 执行反馈 | AI/兽医/农户 |
| 系统支撑 | model_version | 模型版本（含超参/指标） | 训练脚本 |
| | training_dataset | 数据集划分管理 | 训练脚本 |
| | agent_log | Agent 全链路审计 | 论治引擎 |
| | system_config | 系统配置（阈值/模型/参数） | 运营 |
| | dashboard_stats | 看板日统计 | 定时任务 |

**理-法-方-药链落库路径**：

```
syndrome_mapping(多维体征) → syndrome(证候=理+法) → formula(方) → formula_herb(药+剂量)
                                                    └─ add_rule(随症加减) → herb(药物属性)
```

---

*本文档为 2.0 权威口径；《猪咳嗽项目数据库说明文档（诊疗版）.md》与《过时_诊疗决策板块_数据库设计.md》不再维护，仅作迁移参考。*
